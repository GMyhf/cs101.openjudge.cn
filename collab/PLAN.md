# PLAN · 唯一任务清单与决策记录

> 这是 Claude 与 Codex 共享的**唯一任务事实源**。人拍板任务与优先级；
> 两个 agent 认领任务、更新状态、署名。状态流转：`Backlog → In progress → Review → Done`。
> 每条任务用一个 `T-<编号>` 标识，交接与提交信息里引用它。
>
> **格式硬约束**（Redmoon 的教训，见其 2026-07-19 交接）：每行恰好 5 列；
> 追加复核结论写进备注列、用 ` — ` 分隔，**不要**用 `| ` 另起一格——超出表头的
> 单元格在 GitHub 渲染时被直接丢弃，人看到的会是另一份 PLAN。描述里含 `|` 的
> 代码片段要转义（`\|`），否则会把行切碎。

## 状态看板

| ID | 任务 | 状态 | 负责 | 关联提交 / 备注 |
| --- | --- | --- | --- | --- |
| T-000 | 搭建 Claude⇄Codex 协作脚手架（本目录 + `tools/handoff.py`） | Done | Claude | 移植自 Redmoon/collab，红线清单按本项目改写 |
| T-001 | **建立测试套件**：判题核心（AC/WA/TLE/RE/CE、输出比对的 token 语义、资源限制真的生效）+ 服务端（注册/登录/会话、`/api/submit` 未登录 401、静态文件不可穿越）。建成后它取代 `--verify` 成为交接闸门 | Done | Codex | `tests/` 标准库回归套件；`tools/handoff.py --verify` 已委托测试套件并保留语法检查 — **Claude 复核（`097e4f3`）打回两项并直接修好，T-001 收口 11/11**：①闸门在全新克隆上 6/9 失败（判题夹具依赖 `.gitignore` 掉的抓取数据），改为 `tests/fixtures/mirror/` 自带入库夹具 + 服务端提交改用已入库的 `E03406`；②CPU 限额分支是假覆盖（忙等总被 5 秒墙钟抢先，删掉该行测试照样全绿），拆成 `sleep` / 自发 `SIGXCPU` 两个确定性用例。新断言做过变异自检 5/5 全抓；生产代码改动全部认可（路径穿越是实测真 bug）。见本轮 HANDOFF |
| T-026 | **统一题库浏览页**：所有题库入口采用简洁的题目、排名、状态 Tab；排名按本题库已解题数，状态只展示公开的提交摘要 | Done | Codex | 2026-07-29 完成：八个题库根路径与 `/ranking/`、`/status/` 统一使用 `book.html`；公开接口不返回 source/detail。定向回归 2/2、`tools/handoff.py --verify` 通过 |
| T-002 | **为缺测试数据的题目 LLM 生成测试数据**（实现方 = Codex，复核方 = Claude）：按 `tools/make_data_template.py` 模版，为 catalog 中 `test_cases` 为空的题目逐题写参考解法 + 数据生成器，产出 `data/openjudge/tests/<bucket>/<题号>_made/`。**先做 20 题试点批交 Claude 复核，复核通过再分批放量**。详细规格见 `NOTES-claude.md` 2026-07-24 条（二次勘查后已更新：以人的 producecase 工作流为准） | Done | Codex | 20 题试点已生成并完成自动/真实 judge 验收，待 Claude 按五条标准复核 — **Claude 复核试点批（`6add82f`）通过**：样例保真 20/20、双路径复现一致、judge AC 20/20 + 变异 WA 20/20、三题精读算法全对；**抓到并修掉一处真 bug**（`gen_mst` 重复边违反 27880 题面约束，100/100 种子必现，已去重重建，仅 27880 数据变动）。放量批新增两要求（贴上界数据组 + 题面约束逐条过生成器），见 NOTES。~~可以按 ~100 题/批放量~~ **批次规模修订为 ~20 题/轮**（2026-07-24：Codex 自动试做证明生成器必须逐题定制，100 题清单分片为 001a/001b… 消化，68 题「题解可直接执行」优先） — **001a（`7ba5102`）复核：16 过 / 3 打回（假覆盖）/ 4081 补强**。新硬指标：每题去重后 ≥15 组 — **001a-fix（`187cd8e`）复核通过，Claude 又抓并修了 3720 二叉树约束违反（13/20 组含 ≥3 子节点），001a 收口 20/20**。001b 放行，要求自检「题面保证 X → 生成器保证 X」逐条打钩 — **001b（`72532f1`）复核通过收口 20/20**：约束打钩表抽验属实（5442 逐文件核验），Claude 抓并修 5443 最短路多解（108 查询 1 歧义 → 拒绝采样归零）。catalog **1163/1863** 有数据。001c 放行，自检新增「输出在等价解间是否唯一」 — **001c（`38c29f6`）复核通过收口 20/20**：前三轮 bug 形状零复发；Claude 补 6250 的 -1 分支覆盖（重写 g6250 + 独立实现对拍 20/20）。catalog **1209/1863**。001d 放行，自检追加「无解分支必须有数据触发」 — **001d（`8cd5557`）复核：4 题打回、Claude 逐题修好并重建，001d 收口 20/20 → T-002-001 收口（4 批 80 题）**。抓到：①**20123 数据被 `print("YES")` 一行 AC 20/20**（NO 分支 0/20，参考解法有「≥7 位必 YES」捷径而生成范围 19/20 落在捷径里）；②17968 有 13/20 组违反题面「M 为 >=N 的最小素数」；③20018 题面 100% 档 N<=100000 而生成器封顶 80；④16926 M/N/K/T 全未贴上界且漏 K=0、T=0 边界。另**报告自检字段是写死的字面量**（`constraints_checked` 等），001b 立的逐条打钩表被整批删掉——已改为全部从磁盘实测，打钩表填回 10 题、10 题标 null 待 Codex 补。catalog **1254/1863**。第五代自检加「**恒定输出探针**」（最高频输出当解法提交，AC 即无鉴别力）。 — **状态修正（2026-07-25，人指出）**：本条早在 001d 收口时即已完成（4 批 80 题），此前一直误挂 In progress。后续缺数据题目的生成由 T-003（有题解）与 T-004（无题解）承接。 |
| T-003 | **batch-002 选批与生成**：001 消化完后，候选池还剩 ~72 个「有题解 + 有样例」题号，按 001 的成熟流程（选批 → 20 题/轮 → 四代自检 → 复核）继续 | Done | Codex | round5 12/12 完成，候选池 81/81 可构建题全部覆盖，11 题真正 unbuildable 排除；生成器/参考解法 smoke、恒定输出、producecase 字节复现全通过；catalog 实测 1435/1863 — **T-003 收口（5 轮 92 题）**：round5 12 题复核**零缺陷**，AC/变异/恒定输出探针 12/12，独立 fuzz 12 题全清；10 题经 `trim_explanation()` 修正的样例逐题对过输出规格、截断无误。累计生成 **172 题**，catalog **1435/1863**，候选池耗尽。五轮共打回 5 题：22485 孤儿节点、22158 退化树、24588/24591 表达式除零、29702 三分支塌缩；另对账救回 10 题假阴性（讲解粘在样例输出里）+ 人工修 24390 调试残留。 |
| T-004 | **无题解题目**（~288 个缺数据题号）：LLM 自写参考解法 + 生成器。风险高于有题解批（解法本身无人工背书），复核方对自写解法逐题精读，批次规模可能要降到 ~10 题/轮 | Done | Codex | round1-4 共 40 题已生成，round4 返工后待 Claude 复核；3 题独立 oracle，17 题明确 `no_independent_oracle`；catalog 1496/1863 — **round 4 收口（2026-07-25）**：20 题复核通过。经一次打回（17/20 的「独立 oracle」实为参考解法本身）与一次返工后，现 **19/20 有 Python 独立 oracle**（各换算法族，每题 400 种子对拍 0 不一致），4009 因 Θ(2^n) 跑不动样例的 n=20，改用同算法 C++ 交叉验证 case-0 并如实标注。复核另抓出 **3 条空操作变异**（首次替换落在别题分支或死代码上，0/21 组生效），已重定向并立规则「变异必须真的改变行为」。catalog **1496/1863**。**round 5 清单已就绪**（`t004-round5-manifest.json`，20 题 = 原特判跳过的 6 题 + 新选 14 题），仍缺数据题号 276，任务继续。 — **2026-07-26 平台提交轮 + Claude 复核**：Codex 用真实账号把 round4 的 20 份参考解法提交到 cs101.openjudge.cn，**18/20 Accepted**，3728 与 4009 仍平台 TLE（这 2 题的正确性目前只有本地对拍支撑，无平台背书）。Claude 复核发现 4034 被标成`no_independent_oracle` **的根因是脚本里手维护的 `NO_INDEPENDENT_ORACLE` 常量停留在返工前的 17 题旧集合**（其中 16 题 `alt()` 实际都有实现），任何一次重跑都会把报告里的 passed 悄悄覆盖掉；已改为从 `alt()` 推导（`has_oracle`），并让脚本重写报告时原样带走顶层复核记录（此前 `claude_oracle_triage` / `claude_mutation_audit` 两节即因此被冲掉，已恢复）。另抓到**参考解法重写导致 2 条变异退化成空操作**（3728 的 `3*x+1`、3789 的 `range(n-L+1)` 在新实现里已不存在），已重定向。实测：3 题重新对拍各 400 种子 0 不一致，19 题变异全量重扫 0 问题。报告口径 **19 passed + 1 C++ 交叉验证**。 — **2026-07-26 平台验证轮（人拍板沿用到 round5）**：把对拍用的 19 份 oracle 本身也提交到平台，**11 份 Accepted**（真外部背书）、6 TLE、2 MLE（都是为小规模生成数据写的暴力实现，跑不动满规模；不否定本地对拍，但也不提供正确性证据）。**抓到一处硬伤：3725 的 oracle 算的是另一个问题** —— 题面把「从大到小依次放入负荷最小的集合」写死为规则，旧 oracle 却在求全局最优划分，20 万组小输入里 3 组分歧（约 1/67000），400 种子能过纯属分布运气；已按题面过程重写（小根堆 vs 参考的线性扫描），3 组分歧全部一致、400 种子 0 不一致、**平台 Accepted 688ms**。该题**数据不受影响**（由平台 AC 的参考解法生成）。另：3728 与 4009 的 Python 参考在平台 TLE，已补 C++ 并双双 Accepted（17ms / 40ms），round4「这 2 题无 AC 参考实现」一条解除。 — **PyPy3 补测（人拍板加的一档）**：8 份 TLE/MLE 的 oracle 换 PyPy3 **一份都没救回来**（算法层面的暴力，换解释器无用）；但 **3728 与 4009 的 `samplecode.py` 在 PyPy3 档双双 Accepted**（3094ms / 389ms），**推翻了先前「这 2 题没有 AC 的 Python 参考实现」的结论**——算法够，是 CPython 跑不进时限。本地 `judge.py` 不支持 pypy3，故本地仍以 C++ 版为准；是否给判题器加 PyPy3 属红线 1，待人拍板。 — **round5 复核：打回（2026-07-26）**。按新定的三档流程把 20 题参考解法逐题提交平台：**Accepted 11、Wrong Answer 3（3750/4012/4076）、Presentation Error 1（3377）、两档都 TLE 5（4011/4035/4037/4038/27384）**。**那 4 题在本地全部通过**（对拍一致、字节复现、samplecode 复算全对）**却在平台判错——它们生成的 .out 数据是错的**；本地对拍只能证明两个实现彼此一致，证明不了它们解对了题。另抓到：4140 的参考解法与 oracle 是同一个写死的常量（对拍恒真，值本身经二分+牛顿独立验证为对、平台也 Accepted，但流水线没验过），4011 的 oracle 就是参考解法本身（15/27 行逐字相同），`constant_output_probe.status` 退回写死的字面量（001d 立过必须实测），4012 去重仅 4 组（生成器从 3 个写死字符串里挑），3433 缺 samplecode.py 且入库了 64KB ELF 二进制，`NO_INDEPENDENT_ORACLE` 又是手维护常量（round4 刚拆掉的地雷）。详见 `t004-round5-report.json` 的 `claude_review_2026_07_26`。 — **前三轮平台回扫：也有问题（2026-07-26）**。round5 的教训往回做一遍：把 round1-3 共 30 题的参考解法提交平台（此前从未上过平台）。结果 **Accepted 19、失败 8、无法提交 3**：WA 4（3527/3711/3714/3718）、RE 2（3712/3719）、CE 1（3715 —— 参考解法里留着引用未定义变量的死代码 `a[p-1-0] if False else ""`，本地短路不报错、平台 pylint 直接拒）、PE 1（3376，PyPy3 档仍是格式错）。**这三轮此前按「通过」收口，实际有 7 题的数据是错的。**3263/7745/23007 属非 cs101 题库、平台无提交表单，如实记为「检查不适用」。**两轮独立数据：LLM 自写参考解法在本地全部通过的前提下，平台失败率约 20~30%。**详见 `t004-r1to3-platform-check-2026-07-26.json`。 — **Claude 复核 codex 的返工与回扫（2026-07-26）**：前三轮 8 题返工**平台全部 Accepted**（3376 需 PyPy3 档、3718 需 C++），共享模块逐题审计亦全过。T-002/T-003 回扫**169/172**，我独立重提确认 23563 WA、26999 两档 TLE、14683 不适用。**关键对比：题解-backed 批次平台失败率 1/172≈0.6%，LLM 自写批次 20~30%，差两个数量级**——「参考解法的来源」第一次被量化成风险差异。round5 的 4012/4076 已修好并平台 AC，脚本层面的探针字面量与手维护常量已清、已接共享模块，3433 的 ELF 二进制已移除。**仍剩 7 项**：3750 平台 WA、3377 PE（**与 3376 是同一道题、可直接搬已 AC 的实现**）、23563 WA、26999 TLE、4140 与 4011 的 oracle 声称与实现不符、3433 无 samplecode.py。详见 `t004-review-2026-07-26-followup.json`。 — **按人给的 AC 实现修正（2026-07-26）**：23563/26999/03750/04011 换上人提供的平台 Accepted 实现，逐题重新提交**全部 Accepted**；03750 换实现后 21/21 组输出全变（旧数据每组都错）。**03376 已按人拍板移除**（与 03377 同题），catalog 重建后 **1530/1863**。另抓到一条重要区分：**23563 的旧数据本来是对的，平台判 WA 是因为数据挡不住那个 bug**（旧实现的负向后顾排不掉首项零系数，而生成器几乎不产生这种形状）——已补生成器，重建后旧实现被 2/20 组挡住。**平台判 WA 不一定意味着数据错，也可能是数据没有鉴别力。** — **03377 换快实现 → 平台 Accepted（2026-07-26），已知平台失败项全部清零**。原实现每步 `v[i:j+1] <= v[i:j+1][::-1]` 是 O(n²)，题面 N<=30000、本地压测 17.2s 必超时；换成同题 03376 那份平台已 AC 的增量比较实现（0.03s，快 570 倍），两者在 21 组数据上输出完全一致、重建后字节不变 —— **数据本来就是对的**。至此「平台不通过」有了三分法：①实现错→数据错（03750）②实现对但数据挡不住（23563）③实现对但太慢（03377/26999）。**catalog 1530/1863，仍缺数据 258 题，全部有本地题面页。** — **round6 清单已就绪（人拍板开批，2026-07-26）**：`t004-round6-manifest.json`，从仍缺数据的 250 题里取 20 题（4087…4131），**难度 16 低 / 4 中 / 0 高**，逐项验过无数据、无 `_made`、样例齐备、提交路径全部 `/practice/`。跳过 2 题且原因不同：**00000「猫猫测效率」建议永久排除**（I/O 速度测试，输出约 20MB > 判题器 2MB 上限，任何正确解法都判 OLE，放宽上限属红线 1）；03259 可做但**无样例可锚**，平台提交是它唯一的外部验证。交付门槛写进清单：平台 Accepted 才算完成、按题拆单题提交、报告字段走共享模块实测、oracle 换算法族、变异要真的改变行为且基线先绿。 — **round6 复核：部分打回（2026-07-26）**。平台 **14/20 Accepted**，待返工 6 题（4087/4088 MLE、4090/4091/4120 两档 TLE、4114 WA），codex 如实标注未伪装。另 4 处结构性问题：**A** 5 题的 `samplecode.py` 里装的是 C++ 源码（代码本身没问题，编译后跑 21 组 0 不符，纯属放错文件名）；**B** `producecase.py` 内嵌的仍是旧 Python 实现、与换上的参考不同源（数据不受影响，但重跑会用旧实现，一旦旧实现再改就分叉）；**C** 报告 `reference_source` 全写 LLM-written，而其中 6 份已是**其他用户的平台 AC 提交代码**、以 samplecode.py 名义进了公开仓库 —— **出处不实且归属待人拍板**；**D** 4127 去重仅 1 组，题面明写迷宫从 stdin 读、可变，不构成豁免。**4114 根因**：旧实现用 7200 步角度离散扫描近似，外部 AC 用精确叉积；生成器只产平凡构造，所以**外部 AC 对拍 21/21 通过、数据却挡不住平台**。→ **用外部 AC 对拍，若输入仍是我们自己生成的，验证的依然只是「两个实现在我们的数据上一致」，换个更权威的对手不改变对拍的边界。** 详见 `t004-round6-review-2026-07-26.json`。 — **round6 收口（2026-07-26）**：20 题平台全部 Accepted（含 6 题外部平台 AC 实现，已注明统计页/提交 ID/源码链接/许可状态）；`constraint_checklist` 20/20 覆盖（19 题给反例并证明能翻假、4112 如实豁免）。复核抽查从未翻假的 18 条约束中的 9 条，**8 条为真检查、1 条即被豁免那条** —— 说明差的是演示覆盖度而非实质，故**决定不再把契约收紧成「每条各给反例」**（边际收益低于代价）。另修好三条陈旧断言，闸门恢复退出码 0。**仍缺数据 250 题。** — **round7 开批（2026-07-26）**：`t004-round7-manifest.json`，20 题（4132…7592）。按新规范**选批时即查过统计页**：**20/20 全部有既有 Accepted 且都是 Python3**，难度全低，一题都不用自写 —— 起点远好于 round6（自写起步、6 题返工）。跳过 7113 与 3259；**7113 经人拍板已删除**（与 4131 同题，它无数据而 4131 有 21 组），catalog 1856→1855、仍缺数据 **229 题**；3259 题面无样例，可做但无锚点。**另修一处 round6 收尾遗漏：数据落盘后未重建 catalog 索引**，导致这 20 题一直显示「无数据」、对本地判题不可见，且差点让 round7 漏拦同题；已重建 **1527→1557/1856**，候选池 250→**230**。 — **round7 复核通过（2026-07-26）**：20 题全部用平台既有 Python3 Accepted 源码，**我独立重提平台 20/20 Accepted**；共享模块审计 19/20 全清，catalog **1593/1855**。4142 我一度报的三条失败是**自己审计脚本的误报**（没读报告里的豁免、拿清单字面量「无」当样例），codex 的处理正确；该题根值 1.849016 我用 50 位精度二分独立验证一致。**真问题在我建的契约上**：`audit()` 写成 `if constraints is not None`，传 None 就静默跳过约束表 —— 4142 因此整条字段消失。**缺席不该是一种通过方式**，已改为总是写这一项，空表无理由判 FAILED、空表有理由判 exempted。unittest 27→28。 — **round8 开批（2026-07-26）**：`t004-round8-manifest.json`，20 题（7604…15286），**20/20 全部有既有 Python3 Accepted**，难度 18 低 / 2 中。候选池 209 题。**又抓到 3 个同题重复**（07645≡07545、08471≡04122、10716≡04150），三个都是无数据的空边、删起来无迁移成本，**人拍板后已删除**，catalog 1855→1852、仍缺数据 **206 题**。另跳过 3259（题面无样例）。 — **round8 复核通过（2026-07-26）**：20 题全部用平台既有 Python3 Accepted 源码，**我独立重提平台 20/20 Accepted**；共享模块审计 20/20 无问题；**约束反例逐题独立重跑，20/20 与报告一致**（该契约首次被独立复现）；catalog **1616/1852**。另扫出**入库第三方源码有 42 份无署名**（`scripts/t004_platform_accepted_*` 的暂存副本，`_made/samplecode` 那 46 份是齐的），已补齐并加机械检查 `ExternalSourceAttributionTests`（缺来源链接或许可状态即红，变异自检通过）。unittest 28→30。仍缺数据 **186 题**。 — **round9 开批（2026-07-26）**：`t004-round9-manifest.json`，20 题（15291…19974），**20/20 有既有 Accepted**（19 Python3 + 1 G++），难度 18 低 / 2 中，候选池 186 题。**修了选批工具的一个真缺陷**：统计页是分页的，第一版只读第 1 页，把 19974 误判成「无既有 AC」（它实有 22 条 Accepted，全在第 2、3 页）—— 起疑点是题面「通过人数 19」与查找结果对不上。已改为翻页扫描；实测本轮仅此 1 题需要翻页。又抓到 2 个同题重复（15288≡04149、16067≡04151），**人拍板后已删除**，catalog 1852→1849、仍缺数据 **184 题**；同题去重累计消掉 **14 个题号**。 — **round9 复核：19/20 通过，18076 打回（2026-07-27）**。**我独立重提平台 20/20 Accepted**，catalog **1642/1849**，去重/字节复现/参考复算/署名全清。**18076 的 21 组输出全是 `2`**（恒定输出探针 accepted，`print(2)` 即可全过）——codex 的 `self_audit.failed` 已如实记录，但交接摘要写成「全部通过」，**机器字段诚实、散文摘要不诚实**。根因：生成器把两个基团的首原子固定为 6 与 8，**第一步比较就分胜负**，这题的难点（首原子相同、逐层下比、不饱和键）一组未覆盖。实测「首原子相同、后续不同」（题面样例的形状）有鉴别力，而「两分子相同」会让参考实现崩溃、某些长度不等的链会让它挂起 —— **生成器为迁就参考实现而收窄输入，恰恰收进了退化形状**，这是「AC 源码直接当实现」这条规范的代价所在。 — **18076 返工验收通过，round9 收口 20/20（2026-07-27）**：改成「首原子相同、后续结构不同」后，输出从恒定的 `2`×21 变为 `1`×11 / `2`×10，恒定输出探针 accepted→**rejected**，21/21 组首原子相同（题目难点每组都压到），平台复验 **Accepted**。**前后对照最能说明问题**：拿「只比首原子」这个错误实现去撞，旧数据挡住 **0/21**、新数据挡住 **11/21**；而去重、字节复现、复算、约束表在两份数据上结论完全一样 —— **只有恒定输出探针分得出好坏**。仍缺数据 **164 题**。 — **round10 开批（2026-07-27）**：`t004-round10-manifest.json`，20 题（19984…20162），**20/20 有既有 Python3 Accepted**，难度 17 低 / 3 中，候选池 164 题，**本轮无同题重复**。交付门槛新增一条（源自 18076）：**恒定输出探针是唯一验「数据有没有用」的检查**，判 accepted 时先怀疑生成器收窄过头、别急着记豁免；并注明外部 AC 参考只在平台自己的数据上有保证，遇它崩/挂时应调整输入形状而非退到退化形状。 — **round10 复核：18/20 通过，20026 与 20136 打回（2026-07-27）**。**我独立重提平台 20/20 Accepted**，catalog **1666/1849**，**探针 20/20 rejected**（18076 的教训已吃进去）、约束表/字节复现/复算/样例锚点全清。打回两题的去重不达标（20026 只 9 组、20136 只 13 组，均无豁免），逐题查证**都是生成器太窄而非输入域小**：20026 的题面域是 `2^a·3^b`（无穷），生成器最大只到 24；20136 的 21 组**全是环**，而参考实现对星形/链/完全图/分离子图**都跑得好好的**，不存在 18076 那种「参考脆弱只好收窄」的理由。**连续第二轮出现「`self_audit.failed` 诚实、交接摘要写全部通过」**，已建议交接直接列出 failed 题号。 — **round11 已开工（2026-07-27）**：清单 20 题（18 题有平台既有 AC、2 题自写），池子 144→124。**新增一步「用平台 AC 复算清单样例」，20 题查出 5 题样例有问题**（20163 双空格标签错位、21520 两组样例混存、22007 行尾空格、22460 输入截断，以及 **21006——样例干净、是平台那份唯一的 Accepted 本身算错**：`7 3` 应为 8 它输出 5，该题已改判必须自写）。由此补上结构性缺口：`sample_is_case_zero` 原先**只比输入**，导致参考实现只要是错的、六项检查会全绿（其余五项都在拿数据自己跟自己比）。现已加 `sample_output` 比对、不传即 FAILED，`audit()` 透传。**回扫 364 份已交付数据：有样例锚的 345 题输出全部与题面一致**，无一固化错答案。 — **round11 复核：19/20 通过，21520 打回（2026-07-27）**。闸门退出码 0，catalog 1703/1849，**新增的样例输出锚 20/20 passed**（非豁免）、探针 20/20 rejected、约束表 20/20。**我独立提交 19/20 Accepted；21520 平台 TLE 19967ms（#53000269），且报告无平台裁决、无 blocked 标记——违反交接里「拿不到 Accepted 须如实标 blocked」**。另三条待修：①20 题的约束反例全记成占位串 `deliberate invalid input`，**但我取出真 bad 重跑，20 条全部真的会红，检查可靠、坏的是报告**（一行修复）；②21006 的 source_url 指向被否掉的 #52997570，与自己的 reference_source 矛盾；③22007 豁免成立（题面 1<=N<=9）但只生成 N=1..7，漏了输出最大的 8 和 9。 **round11 收尾（df872ad）：四条待办全部改到，20/20 平台通过。**21520 换上已验证的 C++ 参考（我复算 21 组 .out 零不符）、约束反例 20 条独立重跑全部真的会红、21006 source_url 已改、22007 N 覆盖 1..9。改动三题平台复验 3/3 Accepted。一处与摘要不符：摘要称加入 100×100，实际最大仅一组 50×50（另四组 20×25）——改进属实、不必返工，但记录在案。 **round12 已开工（2026-07-27）**：20 题，池子 124→104，**20/20 全有平台既有 Python3 AC、无自写路线**（历轮最好比例）。「用平台 AC 复算样例」已折进选批脚本本身（round11 是事后补做、查出 5 题），本轮 19 题一次通过、1 题（23654 两组样例混存）手工切分后复算确认，现 20/20 全通过。另修难度分级的系统偏差：标题含【难】/【附加题】/Platinum 直接提级，23744、23807、24607 由 low 改判 high。闸门新增三条（反例记真输入 / 拿不到 Accepted 须标 blocked / 摘要数值须从产物读出）。 **round12 复核（e489d71）：质量历轮最好，3 题规模不足。**闸门 0、catalog 1730/1849、failed 全空、样例输出锚与探针与去重与约束表各 20/20，**我独立提交 20/20 Accepted**；新闸门「反例记真实输入」已落实，20 条独立重跑全部真的会红；摘要各项数值均核实无夸大。问题在 3 题的数据规模远低于题面上界：24607（Platinum，题面 N≤3×10⁵，数据 N≤60）、23997（N≤100 vs 34）、23163（n≤110/m≤10000 vs n≤20/m≤30），而参考跑满上界分别只要 2.86s / 0.86s / 0.36s。24607 已做确凿演示：从参考派生的 O(N·K) 慢变体在现有 21 组全对，却在 N=5000 要 17.3s、N=30000 超 45s —— **现有数据不检验复杂度**。 **round13 已开工（2026-07-27）**：20 题，池子 104→84，20/20 全有平台既有 AC（连续第二轮无自写）。上游样例检查抓出 5 题（24830/24837 多组样例标签、25394/27311 样例输出粘解释、25274 题面样例栏写「无」），前四题切分复算确认、25274 记豁免。**25274 的输入是固定字面量，distinct_cases 与探针必然不适用，已在清单写明须走 exemption 而非编造输入。**新增闸门「数据须冲到题面上界附近」（24607 的教训）。另加 stated_bounds 字段但仅命中 6/20，已在清单标明「为空只表示没抽到、不表示无上界」，避免该字段制造覆盖已足的错觉。pending_rework 记 24607/23997/23163。 **round13 复核（e15eb42）：本轮 20 题都好，但 round12 三题返工一组没动。**闸门 0、catalog 1755/1849、failed 全空、样例锚 20/20、**我独立提交 20/20 Accepted**、约束反例 19 条独立重跑全部真的会红；三条豁免依据逐条核对均成立（24755 题面 1<n<13、26144 1<=n<=9、25274 固定字面量）；**规模确已做实**（26267 串到 100 万字符、27311 单组 977KB、26998 数值到 9.99×10⁸）。问题：pending_rework 的 24607/23997/23163 数据完全未动（N≤93 vs 目标 1万~5万、N≤34 vs 100、n≤20 m≤30 vs 110/10000），摘要亦未提及——**这与 21520 同形：自检全绿，坏在自检覆盖不到的那一格**。已建议报告增设 pending_rework_status 机械比对字段。另：豁免题的小域仍未铺满（24755 缺 n=8、26144 缺 n=7），第二次（前有 22007）。 **返工复核（3cbff84）：五项全部属实。**24607 N=50000、23997 N=100、23163 n=110/m=9900、24755 与 26144 的有限域全覆盖；三题返工数据经各自参考复算 0 处不符；**平台复验 5/5 Accepted**。`pending_rework_status` 机制设计正确（从产物读实测、不接受调用方传结论、出错默认 FAILED），我另做变异自检确认它真的会红。**但它原先只在构建时算、未接进 `--verify`——而「没做返工」恰恰意味着数据不会被重建，报告里的绿会一直停在上次。已加 `tools/check_pending_rework.py` 并接入 `run_verify()`：从清单读要求、从产物读现实、每次现算，不读缓存结论；变异验证 `--verify` 确会变红。** **round14 已开工（2026-07-27）**：20 题，池子 84→64，20/20 全有平台既有 AC（连续第三轮无自写）；pending_rework 为空（round13 三条已实测确认完成）。上游样例检查抓出 3 题（27312/28200 及 **27313——样例1 的输入与输出同在「样例输入」栏、样例2 整组在「样例输出」栏**），均切分后用平台 AC 逐组复算确认，现 20/20 通过。**新增 `scripts/t004_submit_round.py`：把平台复验从「复核方记得做」固化成交付方自己能跑的脚本**（口令只读环境变量、逐题兜异常重试 3 次、**「平台判为不通过」与「没提交上去（网络故障）」分开记**，结果写 t004-round{N}-platform.json）。 **round14 复核（0a0f1bc）：27378 与 27778 打回——数据超出题面规格。**闸门 0、catalog 1779/1849、failed 全空、四项自检各 20/20；**codex 自跑新脚本的平台记录我逐条回查，20 条全部属实**（页面 Accepted、题号匹配、本账号）；规模闸门见效（27312 打到 N=200000 上界、27932 单组 1.97MB 数值近 1e9）。问题：27314/27378/27778 的约束退回 `input is present`（反例空串，永远不会红，装饰约束的部分回归；另 17 题反例逐条重跑全部真会红）。**而这个不会红的检查恰好盖住了真缺陷：27378 有 16/21 组的文本含有本不该出现的坏键、27778 有 9/21 组 T 达 11~20 而题面明写 T≤10。**已给出三题现成的可机械检查判据（均验过会红），27378/27778 须重建数据。 **round14 三题修复已独立复验通过（27378/27778/27314 各 21/21 零违反，参考源码确未变）；round15 已开工（2026-07-27）**：20 题，池子 64→44，20/20 全有平台既有 AC（连续第四轮无自写）。上游样例检查抓出 3 题并修复复算：28321（网页为可读性插的空行会让 AC 崩）、28327（三组样例）、**28416（题面三栏均写「同上」，样例取自 28413 并用其自身 AC 复算确认）**。**已标注 28413 与 28416 是同一题的两个规模版本**——去重判据识别不出「正文写同上」，28416 题面原话 T=20、n≤3000，其全部存在理由就是规模，清单已给 scale_requirement 与 machine_gate。新增闸门「约束不能写成永远不会红的形态」（round14 教训）。 **round15 复核（17812a83）：无需打回，最干净的一轮。**闸门 0、catalog 1803/1849、failed 全空、四项自检各 20/20；**退化约束 0 条**（round14 教训落地），20 条反例独立重跑全部真的会红；**我独立从题面推判据查数据合规：8 题逐条比对零越界，且几乎每题精确打到上界**（t=10000、N=2000、n=300、w=500、M=1000、w=99993、T=20）；28416 规模要求达成（T=20、n=3000、单组 476KB，对比 28413 的 T=6/n=40/1KB）；平台记录 20 个 solution_id 逐条回查全部属实。 **round16 已开工（2026-07-27）**：20 题，池子 44→24，20/20 全有平台既有 AC（连续第五轮无自写）。上游样例检查抓出 3 题：29778（题面「每行一个数」而网页样例压成一行，经 AC 读法确认题面为准）、30022（三组样例夹解释）、**29986 猜数为交互题，静态 .in/.out 结构上无法构建，已永久排除**（补位 30085，候补时即复算确认）。**另有一项待人拍板：我们的判题器 CPU 4s，而已交付 465 题里 63 题的平台限时 >4000ms（最高 65536ms）；18250 第 8 组实测 4.46/6.00/5.02s 已踩线，数据完全合规，是判题器比题目本身严 2.5 倍。放宽属红线 1，未动。** **判题器改为按题限时（2026-07-27，人已拍板）**：新增 data/openjudge/limits.json（987 题，已交付 465 题全覆盖）；单组 = clamp(题面限时, 下限 4s, 上限 15s)，**下限保证绝不比改动前更严**；**新增 60s 整次总预算——改动前该项无界（150 组×5s=750s），净效果是暴露面收紧**；FSIZE/AS/PATH 三条不变量未动，7 处变异全被测试抓住。**由此查出 4 题的参考实现在本站过不了**（28321/28405/29340 各 4s TLE、18250 放宽到 10s 仍 TLE——其第 8 组单组需 13.7s 而题面总限时仅 10000ms，是数据太重而非判题器太严），已进 pending_rework。新增 scripts/t004_judge_round.py：与平台复验分工不同，验「这份数据在本站可不可用」。 **判题器补上语言倍率（人给出：Python ×10、PyPy3 ×3、C/C++ ×1；题面限时是所有测试点之和、按 C/C++ 定）。****更正：我此前把 18250 判成「数据太重」是错的——那是漏了倍率**；补上后其 Python 单组预算 20s、整次 100s，实测 Accepted，已从返工撤下。现规则：单组=单点限时（无则总限时）×倍率，夹 [4s,20s]；整次=总限时×倍率，下限「组数×4s」保证不比改动前严、硬顶 300s（改动前最坏 750s）。剩余三题确系数据太重：29340 最慢组 12.2s vs 预算 10s、28321 9.1s vs 4s（单点 300ms×10=3s）、28405 9.6s vs 4s（单点 100ms×10=1s）。 **三题最慢组已压低并重建（改 build_t004_round15.py 的 scale_case，非手改数据，字节复现仍 20/20）**：28321 t 10000→2000（10.4s→1.8s）、28405 n 100000→20000（8.4s→1.8s）、29340 n 10000→4000（10.6s→4.5s），均取到各自预算的一半左右。**本站判题器 20/20 Accepted、平台三题 3/3 Accepted**，自检字段全清。取舍记录：原 scale_case 只照输入上界做（t=10000 确是题面上界），但题面还给了时间预算（28321 每测试点仅 300ms）——**两者都是规格，冲一个不能越另一个**。 **round17 已开工（2026-07-27）**：20 题（19 Python3 + 1 G++），与 round16 零重合。**选批脚本加了 IN_FLIGHT 排除**——第一遍跑出来与 round16 重合 19 题，因为 round16 已开工未交付、那些题在 catalog 里仍标记「无数据」。上游样例检查抓出 3 题：30110、30547（两组样例混存）、**30497 是恶作剧题**（样例输入写着「(此处省略112行)」、样例输出是句玩笑，平台 AC 就是一行 print("undecidable")，王浩瓷砖问题本身不可判定），已给样例锚与恒定输出探针两条豁免并注明不必做规模。新增闸门「题面有两个上界，两个一起看」（round15 的 28321 教训）。**池子见底：本轮取走 20 题后仅剩 31042 一题待做**，另 3 题永久排除（00000 输出超限、3259 无样例、29986 交互题）。 **round17 复核（e383dfef）：本轮质量好，但闸门此前连红四个提交、round16 一题未建。****`--verify` 从 166c50ca「Add dotnet Swift and Objective-C judging」起就红**（language_version 裸调 swiftc，本机没装即 FileNotFoundError，把提交页打成连接直接断），连红至 e383dfef 无人发现；我已修并加 ToolchainProbeTests（bcebea98，变异自检通过）。**codex 本轮摘要未提闸门**——前几轮均写「闸门全绿」。catalog 1823/1849 属实但 +20 全是 round17，**round16 已建 0/20**。round17 本身：failed 全空、样例锚与字节复现各 20/20、本站判题 20/20、**平台记录 20 个 solution_id 逐条回查全部属实**；30497 恶作剧题的三条豁免理由诚实。两处待修：31002 约束退回 nonempty 退化形态（已给可用替换判据并验证会红）、30216 小域缺 n=7（第四次）。 **round16 复核通过（c8b16b93）——最干净的一批。**闸门退出码 0；catalog 1845/1849（99.8%）属实；round16 20/20 已建，failed 全空、样例锚/探针/去重/约束表/字节复现各 20/20（去重零豁免）；**我独立重跑 20 条反例全部真的会红、零退化；本站判题器 20/20；平台记录 20 条逐条回查全部属实**。round17 两处修复到位：30216 域 1..10 全覆盖、31002 换成可证伪判据（反例像真输入）。**盘点：505 个 _made 目录，983/987 题号有数据；池子只剩 31042 一题可做**，另 3 题永久排除（00000 输出超限、3259 无样例、29986 交互题）。是否为 31042 单开一轮待人定。 **round18 已开工（2026-07-27）：最后一题 31042「简化版 Git Diff 引擎」。**既有 Accepted Python3 #52824909，我用它复算题面样例**逐字符相等**；题面给了平局打破规则故输出唯一；平台通过人数仅 2、内存限制 1000kB。**判题器对这题会漏判（人已拍板：按「影响有限」处理，不改）**——它按 token 比对，而本题用行首空格表示「未修改的行」，漏掉行首空格的错误实现在 token 比对下与正确输出完全相同（实测确认），本站会判 Accepted 而平台判 WA。token 比对是判题器核心语义、当初有意设计，改动影响全部 505 份已交付数据；用一处已知的宽松换 505 份数据的判定语义不变，**已定为不改，报告中如实记录**。 **round18（最后一题 31042）复核通过，T-004 收官。**闸门退出码 0；catalog **1846/1849、27,525 组测试数据**属实；去重题号 987 中 984 有数据，缺的正是三个永久排除项 [0, 3259, 29986]。31042：failed 全空、去重 20/20、探针 rejected（最常见输出仅占 5%）、约束反例真实且经我独立重跑确认会红、本站判题 1/1、平台 #53003030 回查属实。**收官前把全部 319 条报告记录扫了一遍，捞出两条陈年遗留**：4140（r5，无输入题，输出恒定是题目决定的——应记豁免、不改数据）与 **15291（r9，真缺陷：20/21 组答案都是 0，数据挡不住「永远输出 0」的程序——须改生成器）**。收尾单见 collab/t004-round19-manifest.json。 **round19 复核通过 —— T-004 全部完成（2026-07-27）。**闸门退出码 0；catalog **1846/1849、27,465 组测试数据**；去重题号 987 中 984 有数据。两条遗留处理方式分得清：4140 压成 1 组并记豁免（题目本身无输入，改数据无意义）、**15291 改生成器（探针最常见输出占比 0.95→0.55，rejected）**；两题本站判题各自 Accepted、平台记录回查属实。**全库 321 条报告记录复扫：failed 非空 0 条。**未覆盖的 3 题均属结构性不可做（00000 输出超限、3259 无样例、29986 交互题）。 |
| T-005 | **Special Judge 支持**：`judge.py` 现在只有 token 精确比对。跳过清单里的浮点容差/多解题（4140、7206、22528、23554、25570、27384 等）需要判题器支持 per-problem 校验器才能有数据。属判题沙箱改动，**红线 1 生效，需人拍板后才动** | Done | Claude | 拍板点：要不要为 ~6+ 题引入 SPJ 机制（每题一个 `checker.py`，跑在与提交代码同级的沙箱里） — **2026-07-25 复核修正（人指出「有的是整数」）**：逐题查证后，**这 6 题没有一个需要 SPJ**。04140 无输入、答案定值、3 段独立题解输出一致；07206 题面规定「路线不唯一时只输出数目」；22528/25570/27384 输出都是唯一整数；23554 两行整数按升序排列 → 唯一。清单原文是「需先确认」（选批暂缓），被我此前概括成「需要 SPJ」，是误读。**T-005 不再是这 6 题的前置条件**：其中 5 题可走常规题解-backed 流程，23554 走 T-004。是否为将来真正的多解题引入 SPJ，是独立取舍。详见 `t002-special-judge-skips.md`。 — **2026-07-27 复量：规模比原记录小得多。**21 题输出以浮点为主，但逐条读题面后**只有 28748 一题真的写了容差**，其余多是「保留 N 位小数」、精确比对够用。**28748 已单独处理**（生成器只产出小数位可终止的局面，循环小数 0 个），不必为一道题引入 SPJ 机制。`tools/full_sweep.py` 已加「浮点循环小数」一项防复发。 |
| T-006 | **前端现代化**（回到项目本意「完整的编写、提交、反馈」）：本地提交页目前是极简 textarea——可做：代码编辑器（语法高亮）、提交历史页（读 submissions 表）、WA 时展示 case 编号 + 期望/实际 token 数之外的更多上下文、题库目录页标注「有数据可判 / 暂无数据」 | Done | Claude | 与 T-003/T-004 完全并行不冲突；拍板后可与数据生成双线推进 — **人拍板收口（2026-07-25）**：四项全部完成。`/problems/` 题库目录页（标注可判/暂无数据）；提交页结构化反馈（**出错的数据组放第一行**）+ 提交历史（`submissions` 表补 book/language/detail 三列）；代码编辑器（无外部依赖：语法高亮、行号、Tab 缩进、**自动缩进、括号匹配、明暗主题**，支持 Python/C/C++）；出错输入片段的**分层开关**——全局默认 → 按题库覆盖 → 考试时段一票否决，默认关、服务端强制、只给输入不给期望输出、坏配置 fail closed，管理页 `/admin/`。unittest 从 11 涨到 21；四轮变异自检共 15/15 全抓。**追加（人拍板）**：自动补全括号/引号、提交历史独立页 `/history/`（统计 + 按题号/结果筛选 + limit 50/200/500）均已完成。后续想法（未做，非阻塞）：按班级或按用户的开关粒度（`settings` 表结构已能容纳，不用改表）。 — **2026-07-27 追加**：导航栏显示在线人数（`SESSION_SEEN` + 5 分钟窗口、按用户名去重、过期打点自动清理）。同时拆掉页面上两个不是从数据来的数字：`/api/stats` 的 `"accepted": 1284, "streak": 12`（凭空编的，当时无页面调用故无人看见）与首页写死的「1,863 收录题目」（catalog 实为 1849，差 14）。`streak` 已删——没有数据能算出它。 |
| T-007 | **已交付数据的全量回归扫描**：自检标准是这一路边做边长出来的（六代），早期批次按当时较弱的标准验收，后立的规矩从未回头套用。对全部 246 个入库 `_made` 题目回溯套用「去重 >=15 / 恒定输出探针 / samplecode 与生成器同源 / 字节复现 / samplecode 逐组复算」五条机械判据 | Done | Claude | 报告 `regression-sweep-2026-07-25-report.json` —— **抓到并修好 8 题**：5 题生成器取值范围比题面窄（4077 题面 n<=15 却封顶 12、4103 题面 20 却封顶 15、4138 题面 S<=10000 却最大 62、4146 漏掉 0 和 100 两端、8581 不去重），3 题输出严重失衡且逐题查证是真缺陷（**9202 的生成器 i=1 时必产生自环，20/20 组违反题面「两个不相等的整数」，还把答案锁死成 Yes**；29803 每图都有直连边被光学迷彩正好抹掉、20/21 组答案恒为 0；3722 有解分支只有 2/21 组）。8 题全部过：去重>=15 + 字节复现 + 样例 + samplecode 复算 + 单组<4s + 恒定输出探针 + 变异自检。**A2 恒定输出探针 246 题全清、B2 逐组复算 245/246**（唯一例外 4009 是已记录情况）。**A3 的 51 个「不同源」逐个 diff 后全是一行头注释，不是缺陷** —— 已撤回。**人已拍板（2026-07-25）**：人手写的 4 份生成器（4102/18250/20741/24729，commit `346c5aa`）用无种子 random、重跑不可复现，**接受现状不改**；B1 口径定为 242/246，那 4 例按已知例外处理。T-007 无遗留项。 |
| T-008 | **判题器支持 PyPy3**：平台有 PyPy3 档而本地 `judge.py` 只有 python3/c/cpp，导致同一份算法正确的 Python 解法「平台能过、本地判 TLE」——本地镜像比真实平台更严。属判题沙箱改动，**红线 1，已获人拍板** | Done | Claude | **沙箱限制一行未动**：PyPy3 走的是同一套 `_limits()`（CPU 4s / FSIZE 2MB / AS 768MB）与同一个 5 秒墙钟，实测在现有限制下即可运行，无需放宽。语法检查交给 PyPy 自己做（本机 PyPy 是 3.9、宿主 CPython 是 3.12，用宿主 `compile()` 代劳会把 CE 误判成 RE）；未安装时返回 `Language Unavailable` 而非崩溃。前端提交页加 PyPy3 选项，高亮表与自动缩进共用 python 那套。**端到端实测：4009 用 python 判 TLE、用 pypy3 判 Accepted（21 组全过）**。unittest 24 → **32**；顺带把红线 1 做成了机械判据 `SandboxContractTests`（限额值钉死 + 每个解释器都必须经过 `_limits`），**8 条变异全抓**（去掉 preexec_fn / CPU 4→8 / 墙钟 5→30 / 内存 768MB→4GB / pypy3 偷偷跑回 CPython / indentFor 漏 pypy3 / SPECS.pypy3 不设 / 下拉去掉选项）。 — **Codex 复核抓到真 bug（`1bd4603`）**：`shutil.which()` 查的是本进程 PATH、子进程拿的是受限 PATH，不一致时裸名字 `FileNotFoundError`，而 `judge()` 只接 `TimeoutExpired`，异常会抛成服务端 500。**Codex 判断正确**。修法改为**把解释器解析成绝对路径**（而非往子进程 PATH 里塞目录——用户代码就跑在那个子进程里，PATH 多一个目录就多一片可执行面）。副产品：子进程 PATH 恒定后沙箱契约才钉得死，新增 `test_child_environment_is_the_fixed_minimal_set`（env 必须恰好是 {PATH, HOME}）与 `test_interpreter_outside_the_child_path_still_runs`。**该回归用例第一版是假覆盖**（shim 名为 pypy3、本机标准 PATH 上就有，退回裸名字照样绿），被变异自检照出后改用空 PATH 才成立。变异 6/6 全抓，unittest 32 → 34。 |
| T-009 | **共享自检模块**：把 001a→round5 一路立起来的判据实现一次，供各轮 build 脚本 import，并把平台提交做成可复用客户端（口令只走环境变量）。人拍板，下一轮从 round4 脚本派生 | Done | Claude | `scripts/t004_common.py`（去重>=15 且不足必须记豁免、恒定输出探针**由测量推出 status**、oracle 独立性分诊、变异必须真的改变行为、字节复现、samplecode 逐组复算、样例即第 0 组）+ `scripts/oj_submit.py`（Python3→PyPy3→C++ 三档；提醒按题拆单题程序，整份分派器会被平台 pylint 判 E0102）+ `tests/test_t004_common.py` **19 条，每条判据都成对写通过/失败路径**。**验收标准：拿模块回扫历史批次** —— round4 报警 0 题（无误报）、round5 精确报出手工复核发现的 3433/4011/4012/4140 四条。round4 脚本已改为 import 共享模块。**副产品：模块上线当天照出一处潜伏回归** —— 3789、3906 的 oracle 在 `6f45e36` 被 Claude 自己误删（重写 3725 时用了「按文本区间整段替换」，两者正好夹在区间里），而报告一直写着它们 passed；已从 `1cb07c9` 找回并重验（题面样例一致、400 种子各 0 不一致）。 |
| T-010 | **提交页力扣化改造**：把 `/practice/<id>/` 改成力扣题目页那种全屏工作台（整页不滚动、左右独立卡片、提交记录移入左侧标签页、编辑器/结果上下分栏且双向可拖），新增「运行样例」（用题面样例先跑一遍再提交），并把设计 token 推广到全站其余 10 个页面。规格与交付门槛见 `collab/t010-manifest.md` | Done | Claude | P1-P4 与本轮 Codex 复核收口：`/api/run` 现在要求题号存在于本地 catalog；`tools/full_sweep.py` 机械扫描 253 道标记式题目的输出首行 `#` 前提；problems/history/admin 已统一顶栏和主题切换。完整闸门通过，见最新 HANDOFF。 |

| T-011 | **服务常驻化**：`deploy/cs101.service` + README 部署段。改动前服务是**孤儿进程**（`setsid nohup` 起、所在 tmux 会话早已死亡、父进程 PID 1），无开机自启、无任何托管 | Done | Claude | 起因：2026-07-28 手工重启**静默失败过一次** —— `pgrep -f "python3 -u server.py"` 会匹配到**执行这条命令的 shell 自己**（命令串里就含这段文字），`kill` 把 shell 一起杀掉、链条断在启动新进程之前，结果老进程活得好好的、看起来却像重启过了；靠「未知题号 `/api/run` 是否返回 404」这个决定性判据才发现。现在发版是 `git pull && sudo systemctl restart cs101`。**踩到并记录的坑**：jensen 的 SELinux 是 Enforcing，`StandardOutput=append:` 指向 home 下的文件会被拒（systemd 以 `init_t` 打开），服务以 `209/STDOUT` 反复重启起不来 —— 已改走 journal，unit 与 README 都写明不要改回去。`KillMode=control-group` 保证停止时带走判题拉起的子进程树；`TimeoutStopSec=20` 不等满 300 秒的判题预算。只加 `PrivateTmp`/`NoNewPrivileges` 两条加固，**不加 `ProtectHome`**（工作目录就在 /home 下，加了起不来）；判题沙箱仍由 `judge.py` 的 `_limits()` 负责，一条未动。**装完实测**：Python 运行样例、C++ 编译运行、完整多测试点判题（Accepted 5 组）全部通过 |

| T-012 | **账号安全加固**：口令哈希改每用户随机盐（旧格式登录时透明升级）、改密/重置口令吊销其它会话、登录失败限速、会话绝对有效期、比较改用 `compare_digest`、上游代理按真实状态码回传（**范围有限，见备注**） | Done | Claude | 起因：T-010 里那个「整个数据库可下载」是**改样式时撞上的，不是有人去看才看到的**，所以对认证面做了一次专门复核。**三条实测确认的问题**：①`password_hash` 全库共用源码里的常量盐 `b"cs101-local-user"` —— 同口令即同哈希（谁和谁口令一样一眼可见），盐公开且唯一，一张彩虹表通吃全库，而这个洞之上数据库一度可直接下载；②**改密不吊销任何会话** —— 实测盗号者的 cookie 在受害者改密之后照样能用，等于「改密码」这个补救动作根本不起作用；③登录**无任何限速**，连续 25 次错误口令全部正常返回 401。另修：会话永不过期（加 14 天绝对有效期）、`valid_password` 与管理员口令用 `==` 比较（改 `compare_digest`）。**向后兼容是硬要求**：线上已有学生账号存的是老格式，`valid_password` 两种格式都认、成功登录时顺手升级，并有端到端测试（直接往库里塞老格式记录再走真实登录）钉住这条。**变异自检 4/4 全抓**：退回固定盐、改密不吊销、去掉限速、老哈希不再被接受，逐条都能让测试变红。限速按用户名而非 IP —— 一个班常共用出口 IP，按 IP 锁会把整班一起锁掉。**一处自我更正**：我原本把「打错 URL 也返回 200」算在本站头上，部署后实测发现**上游 cs101.openjudge.cn 对不存在的路径自己就返回 200（软 404）** —— 改这里只保证「上游真报错时不再被粉饰成 200」，观感上并无变化。因此 T-010 静态白名单测试「断言内容而非状态码」不是将就，是**上游行为决定的、改不掉**，这一点已写进代码注释免得后人再去改回状态码断言 |

| T-013 | **执行类端点的每用户配额**：`/api/run` 5 分钟 30 次、`/api/submit` 10 分钟 20 次 | Done | Claude | 起因：互斥只挡「同时」，挡不住「一直」—— 一个登录用户可以串行地无限次调用 `/api/run` 跑任意代码。沙箱不变（同一套 `_limits()`），限的是频次不是权限。额度按调试节奏定：改一版跑一次，5 分钟 30 次远够用，持续滥用被压到 6 次/分钟。**按用户名计数而非 IP**，与登录限速同一理由（一个班常共用出口 IP）。配额检查放在题号校验**之前**，超额请求不再去解析 4.1MB 的 catalog —— 顺带缓解了 Codex 留下的那个开销取舍。**变异自检 3/3 全抓**：去掉配额、配额改成全局共享、窗口永不过期。**其中第三条一开始没抓住** —— 端到端测试跑不了 5 分钟，看不见「窗口是否真的滑动」，而不清旧记录会让用满一次的用户被**永久**挡住；补了一条直接对纯函数、临时把窗口缩到 1 秒的用例才盖住。 — **追加 `/api/submit` 配额（人要求）**：额度定得比运行样例紧（10 分钟 20 次，即 2 次/分钟持续上限），因为一次提交要跑完全部测试点（最长 300 秒）且会入库留痕。实现从 `run_quota_retry_after` 泛化成 `quota_retry_after(bucket, user, window, limit)`，run 与 submit 各占一个桶。**变异自检又抓到我自己的一条弱断言**：「两个桶互不消耗」原本只试一次 run —— 共用一个桶时剩余空间仍够那一次，测试照样绿；改成把整份 run 额度用满才验得出来，重测确实会红。统一口径：**限的是「请求我们执行」，不是「成功执行」** —— 题号不存在、编译失败一样计数，否则拿无效请求刷同样能占满机器。**另修一处闸门假红**：`tests/test_server.py` 的 `request()` 每请求超时仍是 8 秒，而判题请求要真跑完全部测试点、编译型语言还要先编译，`full_sweep` 之后机器正忙时会超时 —— 本轮实测偶发一次（重跑 3/3 全绿）。服务端启动等待早已因同样原因从 8 秒放宽到 30 秒，这里一直没跟上，现已改为 60 秒 |

| T-014 | **限额可由管理员在线调整 + `/api/user/register` 限频**：`/admin/` 新增「频次限额」面板，三个端点（run / submit / register）的次数与窗口都可改，**次数填 0 表示不限**，存进 `settings` 表、改完立即生效 | Done | Claude | 起因：上一轮那两个数字（30/5分钟、20/10分钟）是拍的、没有使用数据支撑，而**真正会撞上额度的时刻是考试当天，那正是最不能重启的时刻** —— 所以必须能在线改。复用既有 `get_setting`/`set_setting` 与 `/api/settings`（管理员限定），坏值一律 400 拒绝而不是悄悄存下（次数 0..100000、窗口 10..86400 秒）。注册是当时唯一完全没限频的写入端点，验证码挡得住脚本、挡不住慢速刷号，故一并收。**注册只能按来源地址计数**（未登录，没有用户名可依据），而这恰恰有风险：一个班常在同一出口 IP 后面。**这个风险不是推演出来的 —— 加上之后整个测试套件立刻红了**：46 个用例全从 127.0.0.1 注册，20 次/10 分钟当场用尽。据此把默认从 20 放宽到 **100/10 分钟**（只拦批量刷号），并让测试套件在 setUpClass 里显式把注册限频关掉、不再依赖默认值是多少。**变异自检 5/5 全抓**：忽略管理员配置、limit=0 不再表示不限、坏值不再拒绝、注册不限频、非管理员也能改额度。一处自我更正：我给 `/api/submit` 未知题号写的断言抄了 `/api/run` 的 404，实际 submit 是 200 + `status="Problem Not Found"`（只有 run 才 404），已改 |

| T-015 | **`/api/user/forgot` 限频**（同时按邮箱与来源地址两个维度，共用一份可配置额度，默认 10 次/10 分钟）**+ 关掉一处「悄悄降级即任意账号接管」的兜底** | Done | Claude | 限频本身直接：发信端点可被用来对着某人的信箱狂刷重置邮件。**关键实现细节：限频必须写在查库之前、按请求里写的邮箱计数、不管它存不存在** —— 否则只有真实邮箱会被限住，429 本身就成了「这个邮箱注册过」的旁路信号，把该端点原有的「无论存不存在都回同一个 `{ok:true}`」这层保护拆穿。**写测试时撞出一个更严重的既有问题**：断言「真实邮箱与不存在邮箱的响应完全一致」当场就红了 —— 没配 SMTP 时，真实邮箱会**把重置链接直接回给匿名调用者**（`server.py` 原 1838 行），即「知道某人邮箱 = 能接管他的账号」。而触发它的是**悄悄降级**：SMTP 没配、变量名打错、新克隆缺 `data/.smtp.env` 都会掉进来，无任何告警。**线上 jensen 已配 SMTP，该路径未激活**，属潜在而非在线漏洞。已改为必须显式 `CS101_SHOW_RESET_LINK=1` 才回链接，否则只写日志（读日志需管理员权限），对外响应与「邮箱不存在」完全一致。**变异自检 3/3 全抓**：不限频、限频移到查库之后、退回直接回链接。**未动注册的同型兜底**（`activation_link`）：那是给自己刚建的账号发链接，不构成接管既有账号，且本机开发依赖它，已记录待议 |

| T-016 | **修 T-010 引入的回归：C/C++ 编译错误把服务端打成 500** | Done | Claude | `prepare_program` 抽取时，gcc/g++ 分支里那句写成**一行**的 `if rc: return {...}` 没被改成二元组。我当时用的转换正则是 `^\s*return \{"status"`，**锚在行首**，而这句的 `return` 前面还有 `if rc: `，于是漏掉。后果不是「裁定不对」而是**服务端 500**：调用方 `command, failure = prepare_program(...)` 把裸 dict 按**键**解包成 `("status", "message")`，`judge()` 最终返回字符串 `"message"`，服务端再取 `result["status"]` 即 TypeError。**自 `0a1235cf` 起线上生效**，任何学生用 C/C++ 提交带编译错误的代码都拿不到裁定。**为什么全绿了整整一轮**：`tests/test_judge.py` 的编译错误用例只覆盖 python 与 pypy3，两者走的都是解释器分支（那条分支我的正则改对了）；编译型语言分支**一条编译错误用例都没有**。**而且我和 Codex 都核过 `prepare_program`** —— 但我把风险描述成「.NET 分支本机没装 SDK 测不到」，于是双方都只逐行比对了 .NET 那段，**bug 在没人被提醒去看的 C/C++ 分支**。已补两条用例：一条覆盖每一类语言的编译错误，一条**走 AST** 检查 `prepare_program` 每条 return 都是二元组（不用文本匹配 —— 原 bug 正是写成一行，任何「行首是不是 return」的检查都会连同一个盲点一起继承）。变异自检：把那句改回一行式裸 dict，两条用例同时变红 |

| T-017 | **全语言端到端冒烟 + 修 systemd 迁移导致的 PyPy3 失效**：新增 `scripts/smoke_languages.py`，9 种语言各跑一份正确解与一份坏代码 | Done | Claude | 动机：T-010 抽 `prepare_program` 改动了**每一条语言分支**，但此后真正验过的只有 python/pypy3/c/cpp，而 .NET / Swift / ObjC 三条**一次都没跑过** —— 且那正是唯一已知 bug （C/C++ 编译错误 500）所在的同类领域。`tests/test_judge.py` 里这三种的用例都是 `@skipUnless(shutil.which(...))`，在没装工具链的机器上**静默跳过**，闸门全绿不代表验过。**首次运行就抓到一个线上回归，而且是我自己 T-011 引入的**：systemd 不继承登录 shell 的 PATH，而 pypy3 装在 `~/.local/bin`，服务进程 `shutil.which("pypy3")` 返回 None —— **学生选 PyPy3 得到 Language Unavailable**。改用 systemd 托管之前它是从 tmux 登录 shell 起的，PATH 完整所以正常。PyPy3 是 Python 超时唯一的补救档（倍率 ×3 对 ×10），这条失效影响真实。已在单元里补 `Environment=PATH=`。**放宽这里不扩大沙箱**：判题器用服务进程 PATH 找到解释器后，交给子进程的是绝对路径，子进程 env 仍是固定的 `CHILD_PATH`（T-008 的设计）。脚本另加 `--require-all`：工具链缺失算失败而不是跳过 —— **「静默跳过」正是这次回归能藏住的方式**，单元测试和冒烟两处都跳过，就没有任何地方会报告某种语言停止工作了。修复后部署机 `--require-all` **9/9 全部通过**（正确解输出正确、坏代码均得 Compile Error）|

| T-018 | **冒烟纳入发版例行 + 闸门假红排查（未定位，已留现场）** | Done | Claude | ①`tools/release.sh`：拉代码 → 单元有变化则同步并 daemon-reload → 重启 → 等健康 → **全语言冒烟 `--require-all`**，任一步失败即非零退出。冒烟脚本改为像服务端一样从 `data/.admin_password` 读口令，**省掉传口令这一步** —— 有摩擦的例行动作不会被执行。手册另列「代码没变但能力可能变了」的场景（改单元 / 换机器 / 装卸工具链）必须跑冒烟。②闸门假红**根因未定位，但排除了两个假设**：**管道写满被实测证伪**（`log_message` 走 `print` 进 stdout，而测试用 `subprocess.PIPE` 从不排空，写满约 64 KiB 服务端就会阻塞 —— 实测一轮仅约 14 KiB、占 22%，余量充足）；启动等待不足也已排除（早已 30 秒）。连跑 6 轮 + 闸门 1 轮均未复现。**改为把服务端日志写到固定路径 `/tmp/cs101-test-server.log`**：此前是 PIPE，只在启动失败时被读，跑到一半出错则服务端当时在做什么永远丢失 —— 前两次假红正是这么查不下去的（我 `tail -8` 截掉了现场）。顺带也免掉了管道写满这颗雷（今天余量够，但用例只会更多）|

| T-019 | **数据库备份**：`tools/backup_db.py` + `cs101-backup.timer` 每日自动跑 | Done | Claude | `data/course.db` 是这套系统里**唯一不可重建**的东西（代码在 git、题面能重抓、测试数据入库、配置能重写；只有账号与提交记录丢了就是丢了），而此前**没有任何备份机制** —— 无 cron、无 timer、无备份目录。选在此刻做是因为窗口：当时只有 8 用户 / 55 条提交，丢了损失极小；等一个班开始用它就是所有人作业的唯一记录。**备份要在有价值之前建好。** 用 SQLite 在线备份 API 而非 `cp`（服务在跑时 `cp` 可能拷到写了一半的库）；落盘前对**备份文件本身**跑 `integrity_check` 并与源库核对行数；权限 600、存仓库之外（含口令哈希）；默认留 14 份。**写测试时踩到一个坑值得记**：第一次拿**空库**做损坏测试，抹掉 60 字节后 `integrity_check` 照样返回 ok（那片是未使用的页空间）—— 差点得出「校验没用」的错误结论。换成有内容的库、抹掉整整一页才验出来。**损坏测试必须打在有真实内容的库上。** 变异自检 3/3：verify 永远通过 / 不 chmod 600 / 不轮转，均能让用例变红。⚠️ **本地轮转不防这台机器没了**，异地那半需要目标机器，已在手册里留成一条 rsync 命令待运维决定 |
| T-020 | **收紧注册的激活链接兜底 + 服务失败告警** | Done | Claude | ①注册与找回密码两处兜底同形（没配 SMTP 就把链接回给调用者），T-015 只收了后者。重新评估前者：它拿不到别人**既有**账号（邮箱已注册会 409），但确实放开了一件本来做不到的事 —— **拿别人的邮箱注册、无需进对方信箱即可激活**，把地址占掉；邮件正常时占位需要对方信箱里的链接。故一并收紧，两处共用 `CS101_SHOW_ACCOUNT_LINKS`。**测试套件显式打开这个开关**（它靠 activation_link 完成激活），让「这是开发专用行为」在代码里看得见。由此暴露一个测试设计问题：「真实与不存在邮箱响应完全一致」**只在生产配置下成立**，于是新增 `_spawn_server()` 起一个关掉开关的备用服务端来验这条 —— **而不是把断言放松成两种配置下都成立的弱形式**。②`OnFailure=cs101-alert@%n.service`：`Restart=on-failure` 会自动拉起服务，但也让**崩溃循环变成静默的**，自动恢复不该等于无人知晓。告警复用 `data/.smtp.env`，发不出去也写 journal，且不会让告警单元自身失败 |

| T-021 | **并发压测：一个班同时提交会怎样**（`scripts/loadtest_judge.py`）+ 三项修复 | Done | Claude | 动机：判题每组超时是 `cpu_seconds + 1` 而**这个 +1 是墙钟**，机器繁忙时正确代码可能被判 TLE —— 只会在开学第一节课和考试当天暴露，平时 8 个人永远测不出来。**原假设被数据否定**：任何并发下都没出现 TLE（Python ×10 倍率 + 4 秒下限余量充足）。**真正的问题是三个别的**：①**监听队列默认只有 5**（`socketserver.request_queue_size`）—— 同时到达超过 5 个连接，多余的被内核 RST，学生看到「点了没反应」而**服务端日志一条记录都没有**（连接根本没被 accept）。60 并发丢 8 个、100 并发丢 31 个。已改 512。②**fd 软上限 1024 太低** —— 每个并发判题占客户端连接 + 3 个子进程管道 + 临时文件，100 并发实测已用到 644。已加 `LimitNOFILE=65536`。③**判题无全局并发上限**：修掉①之后请求都进得来，全挤在一起跑，**这才出现 TLE**。判题是 CPU 密集的，并发超过核数不提高吞吐、只拉长墙钟，而墙钟正是超时判据 —— 超售换来的不是「慢一点」而是**误判**，还会作为真实裁定写进学生记录。已加 = 核数 的全局闸。**A/B 实测（100 并发，前置条件逐项核对后才开测）**：开闸 100 AC / 0 失败 / 中位 18.3s；不开闸 84 AC / 16 失败 / 中位 22.4s —— **开闸后中位墙钟反而更短**，少了争抢吞吐更好。**方法论教训**：第一次测得出「开闸更差」的相反结论，原因是各档之间只 sleep 3 秒，上一档未结束的判题混进了下一档（`Busy×22` 与 58 个残留判题进程就是证据）。已加「等系统排空再进下一档」，并在开测前核对 backlog/nofile/起始 fd，不满足就拒测而不是拿脏数据下结论 |
| T-022 | **systemd 单元结构检查**（`tests/test_units.py`） | Done | Claude | 起因：给单元加 `LimitNOFILE` 时用朴素字符串替换，而 `Restart=on-failure` 这段文字**同时出现在一条注释里**，替换命中了注释那处 —— `LimitNOFILE` 落进 `[Unit]`（systemd 直接忽略），一条注释被截成非注释行。**服务照常 active、冒烟全过、限制根本没生效**。systemd 在 journal 里写得清清楚楚（`Unknown key name 'LimitNOFILE' in section 'Unit', ignoring`），但没有任何东西逼我去看。现在解析每个单元，检查①指令在能生效的 section 里 ②没有既非注释也非 `Key=Value` 的行 ③这套部署踩坑换来的几条设置都在。**本测试第一版犯了它要防的同一个错**：断言原文不含 `StandardOutput=append:`，结果命中的正是那句「不要用它」的注释 —— 已改为只比对**解析出来的指令**。变异自检 4/4：挪错 section、截断注释、删掉 PATH、加回 append 日志 |

| T-023 | **开发教学手册** `docs/DEV_HANDBOOK.md`：以本判题系统为案例，串起计概/数算/操作系统/网络/数据库/安全/软工/SRE 八门课 | Done | Claude | 形式参照 Redmoon 的 `docs/DEV_HANDBOOK.md`（知识点 → 代码锚点 → 解释 → 课堂讨论/实验）。**本手册的立足点是：这里几乎每一条安全与工程结论，背后都有一次带日期、有实测证据的真实失败** ——固定盐、改密不吊销会话、限频位置导致旁路泄露、fail-open 的重置链接、数据库可被 HTTP 下载、backlog=5 导致连接重置、fd 上限 1024、CPU 超售导致 TLE 误判、`LimitNOFILE` 落错 section 被静默忽略、systemd 不继承 PATH 导致 PyPy3 失效、正则漏改致 C/C++ 编译错误变 500。事故比教条好教，而这些事故的原始记录都在本文件里。另收录三条方法论：「标准立了但没有机械判据等于没立」「永远不会红的检查」「点名风险会让没被点名的地方无人复核」。**全部锚点已机械校验**：17/17 代码符号、3/3 模板内 JS 锚点、14/14 引用文件、12/12 目录锚点均存在。881 行，含 14 个课程实验设计 |

| T-024 | **手册网页版 + GitHub Pages**：`tools/build_handbook.py` 把 `docs/DEV_HANDBOOK.md` 渲染成自包含的 `docs/dev-handbook.html`，另加 `docs/index.html` 落地页 | Done | Claude | 形式参照 Redmoon 的 `tools/build-handbook.mjs`，但**用 Python 标准库重写** —— 本项目的规矩是零第三方依赖，为一份文档引入 markdown 库不划算；只实现手册实际用到的语法子集，子集之外原样输出、不静默吃内容。**标题 id 沿用 GitHub slug 规则**，因为正文目录用的就是 GitHub 风格锚点，换规则会让目录全断；构建时校验每个目录链接都落在真实标题上，对不上直接报错退出。代码引用（`judge.py:100`、`server.py · _limits`）自动链到 GitHub 源码，实测生成 54 个。产出完整 HTML 文档（含 charset/viewport/og 元信息）而非 Redmoon 那种页面片段 —— 这是要公开分享的页面。**新增同步检查**：`tests/test_units.py · HandbookBuildTests` 重新构建并与已提交产物逐字节比 —— 改了 Markdown 忘了重建，公开页会停在旧版本而且毫无征兆（页面照样打得开）。变异自检：改动源文即变红。构建时另发现并修正手册里一处 Markdown 写法错误（粗体写在了行内代码里，GitHub 上同样会渲染成字面量）。另加 `docs/.nojekyll` 关掉 Jekyll，避免中文文件名与下划线前缀被特殊处理 |

| T-025 | **对外开放（Tailscale Funnel）+ 修反代下限频失效** | Done | Claude | 人拍板走「不限注册、靠限频扛」（方案 C）。对外地址 `https://jensen.taildf6cf3.ts.net:8443/`，自带 HTTPS；用 8443 是因为 443 的 `/` 已被本机另一服务（`localhost:8107`）占用，未动它。**开 Funnel 后实测发现一个会让方案 C 失效的问题**：公网来的请求，服务端看到的 `client_address` 是 `127.0.0.1`（代理在同机），照此按 IP 限频等于**把整个互联网算成一个客户端** —— 一个脚本就能耗尽全局额度把所有人挡在门外，而它自己反而不受单独限制。而注册与找回密码是仅有的两个未登录端点，限频是它们唯一的闸。已加 `Handler.client_ip()`：按 `X-Forwarded-For` 取真实地址，**但只在连接来自 `TRUSTED_PROXIES`（本机）时采信** —— 否则直连者加个头即可伪造 IP。变异自检 3/3（无条件采信 / 完全不看 / cookie 不加 Secure）。**验证的坑**：从 yhfbox 测时 MagicDNS 把 `jensen.taildf6cf3.ts.net` 解析成内网地址 `100.123.12.92`，走的是 tailnet 而非公网，第一次测试不算数；改用向公共 DNS 查出 Funnel 公网入口再 `curl --resolve` 强制走它，服务端才记录到真实公网 IP `64.30.64.90`。**测「对外可达」必须真的从外部测。**另：会话 cookie 按 `X-Forwarded-Proto` 加 `Secure`（按请求判断，不影响 tailnet 内 HTTP）；`CS101_PUBLIC_URL` 由内网 IP 改为公网地址，否则**学生收到的激活链接是打不开的地址**。公网端到端实测：8 个学生页面全 200、未登录调 submit/run 均 401、`course.db`/`server.py`/`.admin_password` 均挡住、TLS 校验通过。~~**未验**：激活邮件那条链路需要真实邮箱收信，已交人自测~~ — **人已实测通过（2026-07-28）**：注册 → 收信 → 点激活链接 → 登录 这条链路在公网可用，说明 `CS101_PUBLIC_URL` 改成 `https://jensen.taildf6cf3.ts.net:8443` 之后邮件里的链接是可达的。**至此学生完整旅程全部端到端验证过**：注册/激活/登录（人）+ 找题/提交页/判题/敏感文件防线（自动化）|

## Decision Log

> **对外开放策略（2026-07-28，人拍板选 C）**：不限注册邮箱域名，靠限频与沙箱扛。
> 明确含义：**互联网上任何人都能注册并在这台机器上执行代码**。
> `/api/submit` 与 `/api/run` 均要求登录，所以挡注册即挡代码执行 —— 反过来说不挡即开放算力。
> 沙箱限住单次 CPU/内存/文件，全局并发闸（=核数）保证最坏是排队而非打垮机器，
> 但都挡不住「注册很多号、每个都在额度内」。要收紧，最省力的一处是给注册加邮箱域名白名单。

> **服务改由 systemd 托管（2026-07-28，人拍板）**：此前是孤儿进程 + 手工 `pkill`/`nohup`，
> 且那条手工命令有自匹配缺陷、当天静默失败过一次。现在 `deploy/cs101.service` 入库，
> 发版收敛成 `git pull && sudo systemctl restart cs101`。日志走 journal 而非 home 下的文件 ——
> SELinux Enforcing 下 systemd 打不开它，这一条已写进 unit 注释与 README，避免有人改回去。

> **T-010 三个拍板点（2026-07-28，人拍板）**：①**红线 6 让路一次**——准许新增 `static/theme.css`。
> 判据是红线 6 的字面禁止对象为「框架或依赖」，一个静态 CSS 文件两者都不是；而不让路的代价是
> 4 个平铺 HTML 拿不到共享常量，要么统一失败、要么把它们搬进 `server.py`（再涨约 600 行）。
> 服务端零改动——`server.py:1136-1145` 的兜底路由已能服务 `static/*.css`。
> ②**准许 `POST /api/run` 跑不可信代码，但沙箱不放宽**：沿用 T-008 立的顺序
> 「先测能不能不放宽，再决定要不要放宽」，复用同一套 `_limits()`，必须登录，不写 `submissions` 表。
> ③**并发互斥一并做进 T-010**：`/api/submit` 此前对并发毫无节流（`ThreadingHTTPServer` 每个并发提交
> 直接在 HTTP 线程上拉起编译器），加「运行」按钮会放大该暴露面，故不另开任务、随本轮一起收。

> **T-004 收口覆盖（2026-07-27）**：round19 已完成历史返工，4140 按无输入题豁免并压为 1 组，15291 已扩大生成域并通过恒定输出、本站判题和平台复核。T-004 状态为 **Done**；当前索引为 1846/1849、27,465 组。

> 人拍板的决策记在这里：日期 · 决策 · 理由 / 出处。agent 不得自行改写已有条目。

- 2026-07-26 · **人拍板：平台提交沿用到 round5，并作为交付前的固定一环**（出处：本轮 HANDOFF、
  `t004-round4-report.json` 的 `claude_oracle_platform_check_2026_07_26`）。做法：参考解法写完后，
  除本地对拍外，登录 cs101.openjudge.cn 在对应题目提交，看是否 Accepted。
  **理由（这一轮的实证）**：①3728 与 4009 的 Python 参考解法本地对拍全过、平台却 TLE；
  ②更关键的是 3725 —— 对拍用的 oracle 提交后 Runtime Error，查下去发现它算的根本是另一个问题
  （题面把贪心过程写死为规则，旧 oracle 却在求全局最优划分），20 万组小输入里有 3 组分歧，
  400 种子对拍能过纯属分布运气。**本地对拍只能证明两个实现一致，证明不了它们在解题面要求的那个问题。**
  平台提交是目前唯一能捅破这层的手段。**口令不入库**（红线 2）：只走环境变量，提交脚本留在 scratchpad。
- 2026-07-26 · **人拍板：题目实现优先采用统计页既有 Accepted 代码**。使用用户提供的账号登录对应题目，
  优先寻找语言为 Python3 的 Accepted 提交，其次寻找语言为 G++ 的 Accepted 提交；找到后直接将其作为题目实现并构建测试数据，
  不再自行构建代码做本地对拍。只有找不到 Accepted 代码时，才进行本地对拍和新解法提交。TLE、WA 或未验证源码不能作为平台正确性证据，口令不入库。
- 2026-07-26 · **人拍板：删除 15288 / 16067（同题重复）**（出处：`duplicate-problems-2026-07-26.json`）。
  分别与 04149 / 04151 正文及时间/内存限制逐项一致；两者均无数据无 `_made`，保留方各有 21 组，
  **删的是空的那一边**。删前逐题断言过方向。catalog 1852→1849，仍缺数据题号 186→**184**。
  **至此同题去重累计消掉 14 个题号。** 附记：16067 也在 Claude 早先那份错误的
  「平台无法提交」6 题清单里（和 07645 一样）—— 该清单现已知至少 2 题实为同题重复。

- 2026-07-26 · **人拍板：删除 07645 / 08471 / 10716（同题重复）**
  （出处：`duplicate-problems-2026-07-26.json`、round8 清单）。分别与 07545 / 04122 / 04150
  正文及时间/内存限制逐项一致；三者均无数据无 `_made`，保留方各有 21 组，**删的是空的那一边**、
  无迁移成本。删前逐题断言过方向（要删的确实空、保留的确实有数据）。
  catalog 1855→1852，仍缺数据题号 209→**206**。
  附记：07645 曾被 Claude 误报为「平台无法提交」（实为偶发网络失败）——两个独立的错误指向
  同一题号纯属巧合，但也说明**同题去重与平台可达性是两件事，都得实测**。

- 2026-07-26 · **人拍板：删除 07113（与 04131 同题）**（出处：`duplicate-problems-2026-07-26.json`）。
  正文与时间/内存限制逐项一致；04131 已在 round6 建好 21 组数据、07113 一组没有，故删 07113。
  **这条差点漏掉**：round6 的数据落盘后没重建 catalog 索引，04131 一直显示「无数据」，
  而同题去重是拿「已有数据的题」当基准的 —— 重建索引后才拦住。
  **教训：去重前先确认索引是新的。** catalog 1856→1855，仍缺数据题号 230→**229**。

- 2026-07-26 · **人拍板：清理同题不同号，删 03376 / 04086 / 07810 / 04982 / 12065 / 12066 / 12876 / 24728**
  （出处：`collab/duplicate-problems-2026-07-26.json`）。本地 catalog 把同一道题收在不同 book 下、
  给了不同题号，03376/03377 那次白做了一遍。**判据由人补全：题面正文一致**还不够，
  **时间/内存限制也必须一致** —— 30159 与 30160 正文逐字相同，限制却是 5000ms/单点 4000ms
  对 1000ms（「简版」），是两道题，**都保留**。7 对已逐对验证限制一致。
  **07810 是唯一有数据的**（20 组）而同题的 04107 一组没有，已先把数据迁到 `04107_made` 再删，
  否则会毁掉这道题唯一的一份数据。catalog 1863→1852 条目、1530→1527 有数据、
  **仍缺数据题号 258→251**。**补删（同日）**：03376 此前只删了 `_made` 与脚本引用、
  catalog 条目和页面还留着，已补删 —— 1852→1851 条目、缺数据题号 **251→250**。
  另**逐题实测**了这 250 题能否在 cs101 提交：**只有 6 题拿不到提交表单**
  （07645/08219/16067/21006/23997/25711，全部只属 routine/pctbook/dsapre/2024sp_routine），
  其余 244 题都能走平台闸门。这 6 题若要做须如实标「平台检查不适用」。
  **【本条已作废，见下】**
- 2026-07-26 · **更正：上面那份「不可提交」清单是错的 —— 这 6 题全都能提交**（人指出）。
  我的检查有两处错：①只试 `/practice/` 一条路径，而 07645 在 `/routine/07645/` 下
  （contestId 20380）；②否定结果不复测，251 次连续请求里的偶发失败被当成了结论。
  **方法教训：全量不等于可靠 —— 否定结果必须复测才能当结论。**
  已按人指点抓取 `practice__08219/16067/21006/23997/25711.html` 补进镜像，
  catalog 1851→1856。**重测（逐路径 + 否定复测 3 次）：250 题全部可提交、不可提交 0 题**，
  路径分布 practice 249 / routine 1（07645，contestId 20380）。
  **round6 选批不需要为「平台检查不适用」预留例外。**

- 2026-07-26 · **人拍板：去掉 03376、保留 03377**（出处：本轮 HANDOFF、`t004-round1-report.json`
  的 `removed` 条目）。两者是同一道题 —— 描述/输入/输出/样例输入/样例输出逐段完全相同，
  只是本地 catalog 把它收在不同 book 下、题号不同。03376 的 `_made` 目录、生成器、
  REFERENCE、CONSTRAINTS 与 oracle 分支已全部移除。
- 2026-07-26 · **人提供 6 题的平台 Accepted 参考实现**（23563、26999、04140、03750、04011、03433）。
  已全部落实并逐题重新提交平台验证通过。其中 03750 换实现后 **21/21 组输出全变**（旧数据全错）；
  23563 的旧数据本来是对的，问题是**数据挡不住那个 bug**，已补生成器让它能挡住。

- 2026-07-26 · **人拍板：把反复回来的自检判据抽成共享模块，下一轮 build 脚本从 round4 派生**
  （出处：`scripts/t004_common.py`、`scripts/oj_submit.py`、`tests/test_t004_common.py`）。
  起因：同一批缺陷跨轮反复出现 —— 001d 立的「报告字段必须实测」在 round5 又写成了字面量；
  round4 拆掉的「手维护 oracle 清单」在 round5 又写了一份；001a 立的「去重 >=15」被 T-007
  和 round5 各抓一次。**根因不是粗心，是修复留在旧脚本里，新脚本从零重写就带不过来。**
  落地：判据实现一次放进 `t004_common.py`，接口上**不接受调用方传入实测结果**
  （想把 status 写成字面量在这个接口下做不到）；平台提交客户端放进 `oj_submit.py`，
  口令只走环境变量、不入库。后续批次**从 `build_t004_round4.py` 派生**，不再从零重写。

- 2026-07-26 · **人拍板：给本地判题器加 PyPy3（红线 1 签字），单开 T-008**（出处：T-008、
  `tests/test_judge.py` 的 `SandboxContractTests` 与 `PyPy3Tests`）。起因：平台有 PyPy3 档，
  3728/4009 的 Python 参考解法在 Python3 档 TLE、PyPy3 档 Accepted，而本地 `judge.py` 只支持
  python3/c/cpp —— **同一份算法正确的 Python 解法平台能过、本地判 TLE，本地镜像比真实平台更严**。
  人已在本机安装 pypy3。**落地口径：沙箱限制一行未动** —— PyPy3 与 CPython 共用同一个
  `_limits()`（CPU 4s / FSIZE 2MB / AS 768MB）和同一个 5 秒墙钟；实测 PyPy 在现有限制下就能跑，
  **没有为它放宽任何一项**。这一条此后由 `SandboxContractTests` 机械保证，不再是交接里手写的一句话。

- 2026-07-26 · **人拍板：平台提交增加 PyPy3 一档**（出处：本轮 HANDOFF、
  `t004-round4-report.json` 的 `pypy3_check_2026_07_26`）。做法：Python3 档 TLE 的，
  再用 PyPy3 提交一次再下结论。**实证很直接**：3728 与 4009 的 `samplecode.py` 在 Python3 档 TLE，
  换 PyPy3 双双 Accepted（3094ms / 389ms）——**先前「这 2 题没有 AC 的 Python 参考实现」的结论是错的**，
  算法本身够，只是 CPython 跑不进时限。反过来，8 份 TLE/MLE 的对拍 oracle 换 PyPy3 后**一份都没救回来**，
  确认它们是算法层面的暴力、只适用于小规模生成数据。
  **顺带记一个缺口**：本地 `judge.py` 支持 python3/c/cpp、**不支持 pypy3**，本机也未装。
  于是同一份 Python 参考解法平台 PyPy3 能过、本地判 TLE——就这类题而言本地镜像比真实平台更严。
  是否给本地判题器加 PyPy3 属**判题沙箱改动（红线 1），留待人拍板**。

- 2026-07-26 · **工作规范：所有 `_made` 数据目录及其内容必须纳入 Git**。包括当前已有目录和今后
  新生成的目录；交付生成数据时必须与对应报告、索引变更一起提交并 SSH 推送，不能只提交生成器
  或依赖工作区临时重建。`.gitignore` 仅排除抓取的真实测试数据，已明确豁免
  `data/openjudge/tests/*/*_made/` 及其全部内容。

- 2026-07-26 · **人拍板：Python 参考解法在平台超时就写 C++**（出处：同上）。
  已落地：`03728_made/samplecode_ac.cpp`（Accepted 17ms）、`04009_made/samplecode_ac.cpp`（Accepted 40ms）。
  `samplecode.py` 保留，用于本地生成数据与对拍；平台背书的参考实现以 `samplecode_ac.cpp` 为准。

- 2026-07-25 · **人拍板：`4102`/`18250`/`20741`/`24729` 这 4 份人手写的生成器维持原样，不加种子、
  不重新生成数据**（出处：T-007 回归扫描报告 `regression-sweep-2026-07-25-report.json`、
  本轮 HANDOFF）。事实：这 4 份出自 commit `346c5aa`，`producecase.py` 用的是无种子
  `random`，因此不满足 2026-07-25 立的「重跑 `producecase.py` 后 `data/` 必须逐字节不变」。
  数据本身经 samplecode 逐组复算全部自洽，质量没有问题，只是丢了「生成器 → 数据」的
  可追溯性。理由：加种子就意味着替换掉人自己产出的数据，代价大于收益。
  **落地口径**：B1 字节复现记为 242/246，这 4 题作为**已知且已接受的例外**，
  后续回归扫描跳过它们的 B1 判据，不再计为缺陷。

- 2026-07-25 · **人拍板：26573 不放宽 n 的范围，维持 0..8 共 9 组**（出处：本轮 HANDOFF、
  `scripts/build_t003_002_round3.py` 的 `g26573`）。理由：题面未给 n 上界，输出长度为 3^n，
  判题 2MB 输出上限使 n<=13 才可判；放宽到 0..13 也只有 14 组、仍到不了「≥15 组」，
  却要多约 2.3MB 数据——纯粹是体积换深度，不划算。该题的「去重 9 组 < 15」在报告里
  按**数据体积取舍**记录（`constraints` + `test_cases_note`），**不套用「输入域本身小于 15」的豁免**。

- 2026-07-24 · 协作脚手架落地，流程与文件职责沿用 Redmoon/collab 的形状；
  初始仲裁闸门暂为 `--verify` + 手动冒烟，T-001 完成后由 `tests/` 回归套件接管，
  `--verify` 作为统一入口委托运行测试并继续做语法检查（出处：`tools/handoff.py`）。
- 2026-07-24 · T-001 已决：判题器对 Python 源码先做语法编译，语法错误返回 CE；
  CPU 限额触发的 `SIGXCPU`/`SIGKILL` 返回 TLE；服务端对 URL 解码后的 `..` 路径
  直接返回 404，并支持 `CS101_DB` 注入隔离测试数据库（出处：`judge.py`、`server.py`、
  `tests/test_judge.py`、`tests/test_server.py`）。
- 2026-07-25 · 人拍板去掉 `producecase.py` 的重复存储（出处：`scripts/slim_producecase.py`、
  本轮 HANDOFF）：改回人的模版形状——**生成器 + 固定种子重新生成，不内嵌 CASES**
  （原来同一份输入在仓库里存两份，2.23MB）。全 80 题改写后 `producecase.py`
  2.23MB → 0.37MB，入库 `_made` 数据 10.8MB → 8.92MB。
  交付前自检新增一项：**重跑 `producecase.py` 后 `data/` 必须逐字节不变**。
- 2026-07-25 · 001d 复核后追加第五代自检项（出处：本轮 HANDOFF、`scripts/build_001d.py`）：
  ①**恒定输出探针** —— 把 20 组里频次最高的那份输出原样当解法提交，判 AC 即说明该题数据没有
  鉴别力（20123、6250 都是这个形状，此项可机械抓住）。②**报告自检字段必须是实测值**，
  不得写字面量常量；`constraints` 逐条打钩表是那个布尔唯一的证据，不得省。
  ③选生成范围前先看参考解法有没有 O(1) 捷径分支，范围要能把捷径和主算法都压到。
- 2026-07-25 · T-003 首批复核后追加第六代自检项：对题面定义为图/树/DAG 的生成器，必须在生成器内部用断言验证连通性、节点可达性、遍历序列同树、重边和无环等结构性质；报告只记录实际执行的结构检查，不用泛化布尔代替。
- 2026-07-25 · T-001 复核后补充两条测试纪律（出处：本轮 HANDOFF、`tests/fixtures/mirror/`）：
  ①**闸门必须在全新 `git clone` 上成立** —— 测试不得依赖 `.gitignore` 掉的抓取数据，
  判题夹具自带在 `tests/fixtures/mirror/` 并入库；确需走真判题器时只用 `*_made/` 题号。
  ②**新写的断言要做一次变异自检** —— 把被测那行改坏，确认对应用例真的失败，
  防止「删掉代码测试照样绿」的假覆盖。
- 2026-07-24 · 人拍板 T-002：项目本意是把 cs101.openjudge.cn 现代化，提供完整的
  编写、提交、反馈错在哪组数据的闭环；缺数据的题目用 LLM 生成测试数据，
  目录命名 `<题号>_made`（如 `4102_made`）以标记出处。分工：Codex 生成、Claude 复核。
- 2026-07-24 ·（已决）人的模版落地：`data/openjudge/producecase_prompt/` 37 份单题
  prompt（题面 + producecase_template.py + ac.py → LLM 产出 producecase.py），实物
  样板 `tests/4000-8210/4102_made/`。**以人的工作流为准**，Claude 起草的
  `tools/make_data_template.py` 已删；其样例自检 + 固定种子两条纪律并入 T-002 规格。
- 2026-07-24 · Claude 勘查发现并记录：① 恢复的 tests 树缺 `20000-29982` 桶
  （2236 条 catalog 引用悬空，判题会崩），等人补齐；② 人已生成的 `4102_made`/
  `18250_made` 因数据在 `data/` 子目录而未被索引器收录（`test_count=0`），
  修索引器纳入 T-002 范围。
- 2026-07-24 · 人提供两份人工验证题解合集作为参考解法来源（本机路径见 NOTES-claude
  2026-07-24 放量批条）：覆盖当前 499 个缺数据题号中的 **211 个**。规则：有题解的题
  samplecode.py 必须取自题解（样例断言仍强制），无题解的 288 个题 LLM 自写、排后。
- 2026-07-24 · ~~待人拍板~~ **人已拍板：生成数据入 git（选项 A）**。原问题：`tests/`
  整体在 `.gitignore`，catalog 提交后到重建前判题引用悬空、放量后每台机器都要
  「pull 后跑构建器」。落地：`.gitignore` 改为豁免 `tests/*/*_made/`（抓取的真实
  数据维持不入库），24 个 `_made` 目录（9.5MB 纯文本，含 producecase.py/
  samplecode.py/data）全部入库，仓库自洽。此后 Codex 交付放量批时数据直接随
  commit 走，不再有「需在服务机重建」的间隙。
