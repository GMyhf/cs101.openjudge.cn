# PLAN · 唯一任务清单与决策记录

> 这是 Claude 与 Codex 共享的**唯一任务事实源**。人拍板任务与优先级；
> 两个 agent 认领任务、更新状态、署名。状态流转：`Backlog → In progress → Review → Done`。
> 每条任务用一个 `T-<编号>` 标识，交接与提交信息里引用它。
>
> **格式硬约束**（Redmoon 的教训，见其 2026-07-19 交接）：每行恰好 5 列；
> 追加复核结论写进备注列、用 ` — ` 分隔，**不要**用 `| ` 另起一格——超出表头的
> 单元格在 GitHub 渲染时被直接丢弃，人看到的会是另一份 PLAN。描述里含 `|` 的
> 代码片段要转义（`\|`），否则会把行切碎。

## 状态看板

| ID | 任务 | 状态 | 负责 | 关联提交 / 备注 |
| --- | --- | --- | --- | --- |
| T-000 | 搭建 Claude⇄Codex 协作脚手架（本目录 + `tools/handoff.py`） | Done | Claude | 移植自 Redmoon/collab，红线清单按本项目改写 |
| T-001 | **建立测试套件**：判题核心（AC/WA/TLE/RE/CE、输出比对的 token 语义、资源限制真的生效）+ 服务端（注册/登录/会话、`/api/submit` 未登录 401、静态文件不可穿越）。建成后它取代 `--verify` 成为交接闸门 | Done | Codex | `tests/` 标准库回归套件；`tools/handoff.py --verify` 已委托测试套件并保留语法检查 — **Claude 复核（`097e4f3`）打回两项并直接修好，T-001 收口 11/11**：①闸门在全新克隆上 6/9 失败（判题夹具依赖 `.gitignore` 掉的抓取数据），改为 `tests/fixtures/mirror/` 自带入库夹具 + 服务端提交改用已入库的 `E03406`；②CPU 限额分支是假覆盖（忙等总被 5 秒墙钟抢先，删掉该行测试照样全绿），拆成 `sleep` / 自发 `SIGXCPU` 两个确定性用例。新断言做过变异自检 5/5 全抓；生产代码改动全部认可（路径穿越是实测真 bug）。见本轮 HANDOFF |
| T-002 | **为缺测试数据的题目 LLM 生成测试数据**（实现方 = Codex，复核方 = Claude）：按 `tools/make_data_template.py` 模版，为 catalog 中 `test_cases` 为空的题目逐题写参考解法 + 数据生成器，产出 `data/openjudge/tests/<bucket>/<题号>_made/`。**先做 20 题试点批交 Claude 复核，复核通过再分批放量**。详细规格见 `NOTES-claude.md` 2026-07-24 条（二次勘查后已更新：以人的 producecase 工作流为准） | Done | Codex | 20 题试点已生成并完成自动/真实 judge 验收，待 Claude 按五条标准复核 — **Claude 复核试点批（`6add82f`）通过**：样例保真 20/20、双路径复现一致、judge AC 20/20 + 变异 WA 20/20、三题精读算法全对；**抓到并修掉一处真 bug**（`gen_mst` 重复边违反 27880 题面约束，100/100 种子必现，已去重重建，仅 27880 数据变动）。放量批新增两要求（贴上界数据组 + 题面约束逐条过生成器），见 NOTES。~~可以按 ~100 题/批放量~~ **批次规模修订为 ~20 题/轮**（2026-07-24：Codex 自动试做证明生成器必须逐题定制，100 题清单分片为 001a/001b… 消化，68 题「题解可直接执行」优先） — **001a（`7ba5102`）复核：16 过 / 3 打回（假覆盖）/ 4081 补强**。新硬指标：每题去重后 ≥15 组 — **001a-fix（`187cd8e`）复核通过，Claude 又抓并修了 3720 二叉树约束违反（13/20 组含 ≥3 子节点），001a 收口 20/20**。001b 放行，要求自检「题面保证 X → 生成器保证 X」逐条打钩 — **001b（`72532f1`）复核通过收口 20/20**：约束打钩表抽验属实（5442 逐文件核验），Claude 抓并修 5443 最短路多解（108 查询 1 歧义 → 拒绝采样归零）。catalog **1163/1863** 有数据。001c 放行，自检新增「输出在等价解间是否唯一」 — **001c（`38c29f6`）复核通过收口 20/20**：前三轮 bug 形状零复发；Claude 补 6250 的 -1 分支覆盖（重写 g6250 + 独立实现对拍 20/20）。catalog **1209/1863**。001d 放行，自检追加「无解分支必须有数据触发」 — **001d（`8cd5557`）复核：4 题打回、Claude 逐题修好并重建，001d 收口 20/20 → T-002-001 收口（4 批 80 题）**。抓到：①**20123 数据被 `print("YES")` 一行 AC 20/20**（NO 分支 0/20，参考解法有「≥7 位必 YES」捷径而生成范围 19/20 落在捷径里）；②17968 有 13/20 组违反题面「M 为 >=N 的最小素数」；③20018 题面 100% 档 N<=100000 而生成器封顶 80；④16926 M/N/K/T 全未贴上界且漏 K=0、T=0 边界。另**报告自检字段是写死的字面量**（`constraints_checked` 等），001b 立的逐条打钩表被整批删掉——已改为全部从磁盘实测，打钩表填回 10 题、10 题标 null 待 Codex 补。catalog **1254/1863**。第五代自检加「**恒定输出探针**」（最高频输出当解法提交，AC 即无鉴别力）。 — **状态修正（2026-07-25，人指出）**：本条早在 001d 收口时即已完成（4 批 80 题），此前一直误挂 In progress。后续缺数据题目的生成由 T-003（有题解）与 T-004（无题解）承接。 |
| T-003 | **batch-002 选批与生成**：001 消化完后，候选池还剩 ~72 个「有题解 + 有样例」题号，按 001 的成熟流程（选批 → 20 题/轮 → 四代自检 → 复核）继续 | Done | Codex | round5 12/12 完成，候选池 81/81 可构建题全部覆盖，11 题真正 unbuildable 排除；生成器/参考解法 smoke、恒定输出、producecase 字节复现全通过；catalog 实测 1435/1863 — **T-003 收口（5 轮 92 题）**：round5 12 题复核**零缺陷**，AC/变异/恒定输出探针 12/12，独立 fuzz 12 题全清；10 题经 `trim_explanation()` 修正的样例逐题对过输出规格、截断无误。累计生成 **172 题**，catalog **1435/1863**，候选池耗尽。五轮共打回 5 题：22485 孤儿节点、22158 退化树、24588/24591 表达式除零、29702 三分支塌缩；另对账救回 10 题假阴性（讲解粘在样例输出里）+ 人工修 24390 调试残留。 |
| T-004 | **无题解题目**（~288 个缺数据题号）：LLM 自写参考解法 + 生成器。风险高于有题解批（解法本身无人工背书），复核方对自写解法逐题精读，批次规模可能要降到 ~10 题/轮 | In progress | Codex | round1-4 共 40 题已生成，round4 返工后待 Claude 复核；3 题独立 oracle，17 题明确 `no_independent_oracle`；catalog 1496/1863 — **round 4 收口（2026-07-25）**：20 题复核通过。经一次打回（17/20 的「独立 oracle」实为参考解法本身）与一次返工后，现 **19/20 有 Python 独立 oracle**（各换算法族，每题 400 种子对拍 0 不一致），4009 因 Θ(2^n) 跑不动样例的 n=20，改用同算法 C++ 交叉验证 case-0 并如实标注。复核另抓出 **3 条空操作变异**（首次替换落在别题分支或死代码上，0/21 组生效），已重定向并立规则「变异必须真的改变行为」。catalog **1496/1863**。**round 5 清单已就绪**（`t004-round5-manifest.json`，20 题 = 原特判跳过的 6 题 + 新选 14 题），仍缺数据题号 276，任务继续。 — **2026-07-26 平台提交轮 + Claude 复核**：Codex 用真实账号把 round4 的 20 份参考解法提交到 cs101.openjudge.cn，**18/20 Accepted**，3728 与 4009 仍平台 TLE（这 2 题的正确性目前只有本地对拍支撑，无平台背书）。Claude 复核发现 4034 被标成`no_independent_oracle` **的根因是脚本里手维护的 `NO_INDEPENDENT_ORACLE` 常量停留在返工前的 17 题旧集合**（其中 16 题 `alt()` 实际都有实现），任何一次重跑都会把报告里的 passed 悄悄覆盖掉；已改为从 `alt()` 推导（`has_oracle`），并让脚本重写报告时原样带走顶层复核记录（此前 `claude_oracle_triage` / `claude_mutation_audit` 两节即因此被冲掉，已恢复）。另抓到**参考解法重写导致 2 条变异退化成空操作**（3728 的 `3*x+1`、3789 的 `range(n-L+1)` 在新实现里已不存在），已重定向。实测：3 题重新对拍各 400 种子 0 不一致，19 题变异全量重扫 0 问题。报告口径 **19 passed + 1 C++ 交叉验证**。 — **2026-07-26 平台验证轮（人拍板沿用到 round5）**：把对拍用的 19 份 oracle 本身也提交到平台，**11 份 Accepted**（真外部背书）、6 TLE、2 MLE（都是为小规模生成数据写的暴力实现，跑不动满规模；不否定本地对拍，但也不提供正确性证据）。**抓到一处硬伤：3725 的 oracle 算的是另一个问题** —— 题面把「从大到小依次放入负荷最小的集合」写死为规则，旧 oracle 却在求全局最优划分，20 万组小输入里 3 组分歧（约 1/67000），400 种子能过纯属分布运气；已按题面过程重写（小根堆 vs 参考的线性扫描），3 组分歧全部一致、400 种子 0 不一致、**平台 Accepted 688ms**。该题**数据不受影响**（由平台 AC 的参考解法生成）。另：3728 与 4009 的 Python 参考在平台 TLE，已补 C++ 并双双 Accepted（17ms / 40ms），round4「这 2 题无 AC 参考实现」一条解除。 — **PyPy3 补测（人拍板加的一档）**：8 份 TLE/MLE 的 oracle 换 PyPy3 **一份都没救回来**（算法层面的暴力，换解释器无用）；但 **3728 与 4009 的 `samplecode.py` 在 PyPy3 档双双 Accepted**（3094ms / 389ms），**推翻了先前「这 2 题没有 AC 的 Python 参考实现」的结论**——算法够，是 CPython 跑不进时限。本地 `judge.py` 不支持 pypy3，故本地仍以 C++ 版为准；是否给判题器加 PyPy3 属红线 1，待人拍板。 — **round5 复核：打回（2026-07-26）**。按新定的三档流程把 20 题参考解法逐题提交平台：**Accepted 11、Wrong Answer 3（3750/4012/4076）、Presentation Error 1（3377）、两档都 TLE 5（4011/4035/4037/4038/27384）**。**那 4 题在本地全部通过**（对拍一致、字节复现、samplecode 复算全对）**却在平台判错——它们生成的 .out 数据是错的**；本地对拍只能证明两个实现彼此一致，证明不了它们解对了题。另抓到：4140 的参考解法与 oracle 是同一个写死的常量（对拍恒真，值本身经二分+牛顿独立验证为对、平台也 Accepted，但流水线没验过），4011 的 oracle 就是参考解法本身（15/27 行逐字相同），`constant_output_probe.status` 退回写死的字面量（001d 立过必须实测），4012 去重仅 4 组（生成器从 3 个写死字符串里挑），3433 缺 samplecode.py 且入库了 64KB ELF 二进制，`NO_INDEPENDENT_ORACLE` 又是手维护常量（round4 刚拆掉的地雷）。详见 `t004-round5-report.json` 的 `claude_review_2026_07_26`。 — **前三轮平台回扫：也有问题（2026-07-26）**。round5 的教训往回做一遍：把 round1-3 共 30 题的参考解法提交平台（此前从未上过平台）。结果 **Accepted 19、失败 8、无法提交 3**：WA 4（3527/3711/3714/3718）、RE 2（3712/3719）、CE 1（3715 —— 参考解法里留着引用未定义变量的死代码 `a[p-1-0] if False else ""`，本地短路不报错、平台 pylint 直接拒）、PE 1（3376，PyPy3 档仍是格式错）。**这三轮此前按「通过」收口，实际有 7 题的数据是错的。**3263/7745/23007 属非 cs101 题库、平台无提交表单，如实记为「检查不适用」。**两轮独立数据：LLM 自写参考解法在本地全部通过的前提下，平台失败率约 20~30%。**详见 `t004-r1to3-platform-check-2026-07-26.json`。 — **Claude 复核 codex 的返工与回扫（2026-07-26）**：前三轮 8 题返工**平台全部 Accepted**（3376 需 PyPy3 档、3718 需 C++），共享模块逐题审计亦全过。T-002/T-003 回扫**169/172**，我独立重提确认 23563 WA、26999 两档 TLE、14683 不适用。**关键对比：题解-backed 批次平台失败率 1/172≈0.6%，LLM 自写批次 20~30%，差两个数量级**——「参考解法的来源」第一次被量化成风险差异。round5 的 4012/4076 已修好并平台 AC，脚本层面的探针字面量与手维护常量已清、已接共享模块，3433 的 ELF 二进制已移除。**仍剩 7 项**：3750 平台 WA、3377 PE（**与 3376 是同一道题、可直接搬已 AC 的实现**）、23563 WA、26999 TLE、4140 与 4011 的 oracle 声称与实现不符、3433 无 samplecode.py。详见 `t004-review-2026-07-26-followup.json`。 — **按人给的 AC 实现修正（2026-07-26）**：23563/26999/03750/04011 换上人提供的平台 Accepted 实现，逐题重新提交**全部 Accepted**；03750 换实现后 21/21 组输出全变（旧数据每组都错）。**03376 已按人拍板移除**（与 03377 同题），catalog 重建后 **1530/1863**。另抓到一条重要区分：**23563 的旧数据本来是对的，平台判 WA 是因为数据挡不住那个 bug**（旧实现的负向后顾排不掉首项零系数，而生成器几乎不产生这种形状）——已补生成器，重建后旧实现被 2/20 组挡住。**平台判 WA 不一定意味着数据错，也可能是数据没有鉴别力。** — **03377 换快实现 → 平台 Accepted（2026-07-26），已知平台失败项全部清零**。原实现每步 `v[i:j+1] <= v[i:j+1][::-1]` 是 O(n²)，题面 N<=30000、本地压测 17.2s 必超时；换成同题 03376 那份平台已 AC 的增量比较实现（0.03s，快 570 倍），两者在 21 组数据上输出完全一致、重建后字节不变 —— **数据本来就是对的**。至此「平台不通过」有了三分法：①实现错→数据错（03750）②实现对但数据挡不住（23563）③实现对但太慢（03377/26999）。**catalog 1530/1863，仍缺数据 258 题，全部有本地题面页。** — **round6 清单已就绪（人拍板开批，2026-07-26）**：`t004-round6-manifest.json`，从仍缺数据的 250 题里取 20 题（4087…4131），**难度 16 低 / 4 中 / 0 高**，逐项验过无数据、无 `_made`、样例齐备、提交路径全部 `/practice/`。跳过 2 题且原因不同：**00000「猫猫测效率」建议永久排除**（I/O 速度测试，输出约 20MB > 判题器 2MB 上限，任何正确解法都判 OLE，放宽上限属红线 1）；03259 可做但**无样例可锚**，平台提交是它唯一的外部验证。交付门槛写进清单：平台 Accepted 才算完成、按题拆单题提交、报告字段走共享模块实测、oracle 换算法族、变异要真的改变行为且基线先绿。 — **round6 复核：部分打回（2026-07-26）**。平台 **14/20 Accepted**，待返工 6 题（4087/4088 MLE、4090/4091/4120 两档 TLE、4114 WA），codex 如实标注未伪装。另 4 处结构性问题：**A** 5 题的 `samplecode.py` 里装的是 C++ 源码（代码本身没问题，编译后跑 21 组 0 不符，纯属放错文件名）；**B** `producecase.py` 内嵌的仍是旧 Python 实现、与换上的参考不同源（数据不受影响，但重跑会用旧实现，一旦旧实现再改就分叉）；**C** 报告 `reference_source` 全写 LLM-written，而其中 6 份已是**其他用户的平台 AC 提交代码**、以 samplecode.py 名义进了公开仓库 —— **出处不实且归属待人拍板**；**D** 4127 去重仅 1 组，题面明写迷宫从 stdin 读、可变，不构成豁免。**4114 根因**：旧实现用 7200 步角度离散扫描近似，外部 AC 用精确叉积；生成器只产平凡构造，所以**外部 AC 对拍 21/21 通过、数据却挡不住平台**。→ **用外部 AC 对拍，若输入仍是我们自己生成的，验证的依然只是「两个实现在我们的数据上一致」，换个更权威的对手不改变对拍的边界。** 详见 `t004-round6-review-2026-07-26.json`。 — **round6 收口（2026-07-26）**：20 题平台全部 Accepted（含 6 题外部平台 AC 实现，已注明统计页/提交 ID/源码链接/许可状态）；`constraint_checklist` 20/20 覆盖（19 题给反例并证明能翻假、4112 如实豁免）。复核抽查从未翻假的 18 条约束中的 9 条，**8 条为真检查、1 条即被豁免那条** —— 说明差的是演示覆盖度而非实质，故**决定不再把契约收紧成「每条各给反例」**（边际收益低于代价）。另修好三条陈旧断言，闸门恢复退出码 0。**仍缺数据 250 题。** — **round7 开批（2026-07-26）**：`t004-round7-manifest.json`，20 题（4132…7592）。按新规范**选批时即查过统计页**：**20/20 全部有既有 Accepted 且都是 Python3**，难度全低，一题都不用自写 —— 起点远好于 round6（自写起步、6 题返工）。跳过 7113 与 3259；**7113 经人拍板已删除**（与 4131 同题，它无数据而 4131 有 21 组），catalog 1856→1855、仍缺数据 **229 题**；3259 题面无样例，可做但无锚点。**另修一处 round6 收尾遗漏：数据落盘后未重建 catalog 索引**，导致这 20 题一直显示「无数据」、对本地判题不可见，且差点让 round7 漏拦同题；已重建 **1527→1557/1856**，候选池 250→**230**。 — **round7 复核通过（2026-07-26）**：20 题全部用平台既有 Python3 Accepted 源码，**我独立重提平台 20/20 Accepted**；共享模块审计 19/20 全清，catalog **1593/1855**。4142 我一度报的三条失败是**自己审计脚本的误报**（没读报告里的豁免、拿清单字面量「无」当样例），codex 的处理正确；该题根值 1.849016 我用 50 位精度二分独立验证一致。**真问题在我建的契约上**：`audit()` 写成 `if constraints is not None`，传 None 就静默跳过约束表 —— 4142 因此整条字段消失。**缺席不该是一种通过方式**，已改为总是写这一项，空表无理由判 FAILED、空表有理由判 exempted。unittest 27→28。 |
| T-005 | **Special Judge 支持**：`judge.py` 现在只有 token 精确比对。跳过清单里的浮点容差/多解题（4140、7206、22528、23554、25570、27384 等）需要判题器支持 per-problem 校验器才能有数据。属判题沙箱改动，**红线 1 生效，需人拍板后才动** | Backlog | — | 拍板点：要不要为 ~6+ 题引入 SPJ 机制（每题一个 `checker.py`，跑在与提交代码同级的沙箱里） — **2026-07-25 复核修正（人指出「有的是整数」）**：逐题查证后，**这 6 题没有一个需要 SPJ**。04140 无输入、答案定值、3 段独立题解输出一致；07206 题面规定「路线不唯一时只输出数目」；22528/25570/27384 输出都是唯一整数；23554 两行整数按升序排列 → 唯一。清单原文是「需先确认」（选批暂缓），被我此前概括成「需要 SPJ」，是误读。**T-005 不再是这 6 题的前置条件**：其中 5 题可走常规题解-backed 流程，23554 走 T-004。是否为将来真正的多解题引入 SPJ，是独立取舍。详见 `t002-special-judge-skips.md`。 |
| T-006 | **前端现代化**（回到项目本意「完整的编写、提交、反馈」）：本地提交页目前是极简 textarea——可做：代码编辑器（语法高亮）、提交历史页（读 submissions 表）、WA 时展示 case 编号 + 期望/实际 token 数之外的更多上下文、题库目录页标注「有数据可判 / 暂无数据」 | Done | Claude | 与 T-003/T-004 完全并行不冲突；拍板后可与数据生成双线推进 — **人拍板收口（2026-07-25）**：四项全部完成。`/problems/` 题库目录页（标注可判/暂无数据）；提交页结构化反馈（**出错的数据组放第一行**）+ 提交历史（`submissions` 表补 book/language/detail 三列）；代码编辑器（无外部依赖：语法高亮、行号、Tab 缩进、**自动缩进、括号匹配、明暗主题**，支持 Python/C/C++）；出错输入片段的**分层开关**——全局默认 → 按题库覆盖 → 考试时段一票否决，默认关、服务端强制、只给输入不给期望输出、坏配置 fail closed，管理页 `/admin/`。unittest 从 11 涨到 21；四轮变异自检共 15/15 全抓。**追加（人拍板）**：自动补全括号/引号、提交历史独立页 `/history/`（统计 + 按题号/结果筛选 + limit 50/200/500）均已完成。后续想法（未做，非阻塞）：按班级或按用户的开关粒度（`settings` 表结构已能容纳，不用改表）。 |
| T-007 | **已交付数据的全量回归扫描**：自检标准是这一路边做边长出来的（六代），早期批次按当时较弱的标准验收，后立的规矩从未回头套用。对全部 246 个入库 `_made` 题目回溯套用「去重 >=15 / 恒定输出探针 / samplecode 与生成器同源 / 字节复现 / samplecode 逐组复算」五条机械判据 | Done | Claude | 报告 `regression-sweep-2026-07-25-report.json` —— **抓到并修好 8 题**：5 题生成器取值范围比题面窄（4077 题面 n<=15 却封顶 12、4103 题面 20 却封顶 15、4138 题面 S<=10000 却最大 62、4146 漏掉 0 和 100 两端、8581 不去重），3 题输出严重失衡且逐题查证是真缺陷（**9202 的生成器 i=1 时必产生自环，20/20 组违反题面「两个不相等的整数」，还把答案锁死成 Yes**；29803 每图都有直连边被光学迷彩正好抹掉、20/21 组答案恒为 0；3722 有解分支只有 2/21 组）。8 题全部过：去重>=15 + 字节复现 + 样例 + samplecode 复算 + 单组<4s + 恒定输出探针 + 变异自检。**A2 恒定输出探针 246 题全清、B2 逐组复算 245/246**（唯一例外 4009 是已记录情况）。**A3 的 51 个「不同源」逐个 diff 后全是一行头注释，不是缺陷** —— 已撤回。**人已拍板（2026-07-25）**：人手写的 4 份生成器（4102/18250/20741/24729，commit `346c5aa`）用无种子 random、重跑不可复现，**接受现状不改**；B1 口径定为 242/246，那 4 例按已知例外处理。T-007 无遗留项。 |
| T-008 | **判题器支持 PyPy3**：平台有 PyPy3 档而本地 `judge.py` 只有 python3/c/cpp，导致同一份算法正确的 Python 解法「平台能过、本地判 TLE」——本地镜像比真实平台更严。属判题沙箱改动，**红线 1，已获人拍板** | Done | Claude | **沙箱限制一行未动**：PyPy3 走的是同一套 `_limits()`（CPU 4s / FSIZE 2MB / AS 768MB）与同一个 5 秒墙钟，实测在现有限制下即可运行，无需放宽。语法检查交给 PyPy 自己做（本机 PyPy 是 3.9、宿主 CPython 是 3.12，用宿主 `compile()` 代劳会把 CE 误判成 RE）；未安装时返回 `Language Unavailable` 而非崩溃。前端提交页加 PyPy3 选项，高亮表与自动缩进共用 python 那套。**端到端实测：4009 用 python 判 TLE、用 pypy3 判 Accepted（21 组全过）**。unittest 24 → **32**；顺带把红线 1 做成了机械判据 `SandboxContractTests`（限额值钉死 + 每个解释器都必须经过 `_limits`），**8 条变异全抓**（去掉 preexec_fn / CPU 4→8 / 墙钟 5→30 / 内存 768MB→4GB / pypy3 偷偷跑回 CPython / indentFor 漏 pypy3 / SPECS.pypy3 不设 / 下拉去掉选项）。 — **Codex 复核抓到真 bug（`1bd4603`）**：`shutil.which()` 查的是本进程 PATH、子进程拿的是受限 PATH，不一致时裸名字 `FileNotFoundError`，而 `judge()` 只接 `TimeoutExpired`，异常会抛成服务端 500。**Codex 判断正确**。修法改为**把解释器解析成绝对路径**（而非往子进程 PATH 里塞目录——用户代码就跑在那个子进程里，PATH 多一个目录就多一片可执行面）。副产品：子进程 PATH 恒定后沙箱契约才钉得死，新增 `test_child_environment_is_the_fixed_minimal_set`（env 必须恰好是 {PATH, HOME}）与 `test_interpreter_outside_the_child_path_still_runs`。**该回归用例第一版是假覆盖**（shim 名为 pypy3、本机标准 PATH 上就有，退回裸名字照样绿），被变异自检照出后改用空 PATH 才成立。变异 6/6 全抓，unittest 32 → 34。 |
| T-009 | **共享自检模块**：把 001a→round5 一路立起来的判据实现一次，供各轮 build 脚本 import，并把平台提交做成可复用客户端（口令只走环境变量）。人拍板，下一轮从 round4 脚本派生 | Done | Claude | `scripts/t004_common.py`（去重>=15 且不足必须记豁免、恒定输出探针**由测量推出 status**、oracle 独立性分诊、变异必须真的改变行为、字节复现、samplecode 逐组复算、样例即第 0 组）+ `scripts/oj_submit.py`（Python3→PyPy3→C++ 三档；提醒按题拆单题程序，整份分派器会被平台 pylint 判 E0102）+ `tests/test_t004_common.py` **19 条，每条判据都成对写通过/失败路径**。**验收标准：拿模块回扫历史批次** —— round4 报警 0 题（无误报）、round5 精确报出手工复核发现的 3433/4011/4012/4140 四条。round4 脚本已改为 import 共享模块。**副产品：模块上线当天照出一处潜伏回归** —— 3789、3906 的 oracle 在 `6f45e36` 被 Claude 自己误删（重写 3725 时用了「按文本区间整段替换」，两者正好夹在区间里），而报告一直写着它们 passed；已从 `1cb07c9` 找回并重验（题面样例一致、400 种子各 0 不一致）。 |

## Decision Log

> 人拍板的决策记在这里：日期 · 决策 · 理由 / 出处。agent 不得自行改写已有条目。

- 2026-07-26 · **人拍板：平台提交沿用到 round5，并作为交付前的固定一环**（出处：本轮 HANDOFF、
  `t004-round4-report.json` 的 `claude_oracle_platform_check_2026_07_26`）。做法：参考解法写完后，
  除本地对拍外，登录 cs101.openjudge.cn 在对应题目提交，看是否 Accepted。
  **理由（这一轮的实证）**：①3728 与 4009 的 Python 参考解法本地对拍全过、平台却 TLE；
  ②更关键的是 3725 —— 对拍用的 oracle 提交后 Runtime Error，查下去发现它算的根本是另一个问题
  （题面把贪心过程写死为规则，旧 oracle 却在求全局最优划分），20 万组小输入里有 3 组分歧，
  400 种子对拍能过纯属分布运气。**本地对拍只能证明两个实现一致，证明不了它们在解题面要求的那个问题。**
  平台提交是目前唯一能捅破这层的手段。**口令不入库**（红线 2）：只走环境变量，提交脚本留在 scratchpad。
- 2026-07-26 · **人拍板：题目实现优先采用统计页既有 Accepted 代码**。使用用户提供的账号登录对应题目，
  优先寻找语言为 Python3 的 Accepted 提交，其次寻找语言为 G++ 的 Accepted 提交；找到后直接将其作为题目实现并构建测试数据，
  不再自行构建代码做本地对拍。只有找不到 Accepted 代码时，才进行本地对拍和新解法提交。TLE、WA 或未验证源码不能作为平台正确性证据，口令不入库。
- 2026-07-26 · **人拍板：删除 07113（与 04131 同题）**（出处：`duplicate-problems-2026-07-26.json`）。
  正文与时间/内存限制逐项一致；04131 已在 round6 建好 21 组数据、07113 一组没有，故删 07113。
  **这条差点漏掉**：round6 的数据落盘后没重建 catalog 索引，04131 一直显示「无数据」，
  而同题去重是拿「已有数据的题」当基准的 —— 重建索引后才拦住。
  **教训：去重前先确认索引是新的。** catalog 1856→1855，仍缺数据题号 230→**229**。

- 2026-07-26 · **人拍板：清理同题不同号，删 03376 / 04086 / 07810 / 04982 / 12065 / 12066 / 12876 / 24728**
  （出处：`collab/duplicate-problems-2026-07-26.json`）。本地 catalog 把同一道题收在不同 book 下、
  给了不同题号，03376/03377 那次白做了一遍。**判据由人补全：题面正文一致**还不够，
  **时间/内存限制也必须一致** —— 30159 与 30160 正文逐字相同，限制却是 5000ms/单点 4000ms
  对 1000ms（「简版」），是两道题，**都保留**。7 对已逐对验证限制一致。
  **07810 是唯一有数据的**（20 组）而同题的 04107 一组没有，已先把数据迁到 `04107_made` 再删，
  否则会毁掉这道题唯一的一份数据。catalog 1863→1852 条目、1530→1527 有数据、
  **仍缺数据题号 258→251**。**补删（同日）**：03376 此前只删了 `_made` 与脚本引用、
  catalog 条目和页面还留着，已补删 —— 1852→1851 条目、缺数据题号 **251→250**。
  另**逐题实测**了这 250 题能否在 cs101 提交：**只有 6 题拿不到提交表单**
  （07645/08219/16067/21006/23997/25711，全部只属 routine/pctbook/dsapre/2024sp_routine），
  其余 244 题都能走平台闸门。这 6 题若要做须如实标「平台检查不适用」。
  **【本条已作废，见下】**
- 2026-07-26 · **更正：上面那份「不可提交」清单是错的 —— 这 6 题全都能提交**（人指出）。
  我的检查有两处错：①只试 `/practice/` 一条路径，而 07645 在 `/routine/07645/` 下
  （contestId 20380）；②否定结果不复测，251 次连续请求里的偶发失败被当成了结论。
  **方法教训：全量不等于可靠 —— 否定结果必须复测才能当结论。**
  已按人指点抓取 `practice__08219/16067/21006/23997/25711.html` 补进镜像，
  catalog 1851→1856。**重测（逐路径 + 否定复测 3 次）：250 题全部可提交、不可提交 0 题**，
  路径分布 practice 249 / routine 1（07645，contestId 20380）。
  **round6 选批不需要为「平台检查不适用」预留例外。**

- 2026-07-26 · **人拍板：去掉 03376、保留 03377**（出处：本轮 HANDOFF、`t004-round1-report.json`
  的 `removed` 条目）。两者是同一道题 —— 描述/输入/输出/样例输入/样例输出逐段完全相同，
  只是本地 catalog 把它收在不同 book 下、题号不同。03376 的 `_made` 目录、生成器、
  REFERENCE、CONSTRAINTS 与 oracle 分支已全部移除。
- 2026-07-26 · **人提供 6 题的平台 Accepted 参考实现**（23563、26999、04140、03750、04011、03433）。
  已全部落实并逐题重新提交平台验证通过。其中 03750 换实现后 **21/21 组输出全变**（旧数据全错）；
  23563 的旧数据本来是对的，问题是**数据挡不住那个 bug**，已补生成器让它能挡住。

- 2026-07-26 · **人拍板：把反复回来的自检判据抽成共享模块，下一轮 build 脚本从 round4 派生**
  （出处：`scripts/t004_common.py`、`scripts/oj_submit.py`、`tests/test_t004_common.py`）。
  起因：同一批缺陷跨轮反复出现 —— 001d 立的「报告字段必须实测」在 round5 又写成了字面量；
  round4 拆掉的「手维护 oracle 清单」在 round5 又写了一份；001a 立的「去重 >=15」被 T-007
  和 round5 各抓一次。**根因不是粗心，是修复留在旧脚本里，新脚本从零重写就带不过来。**
  落地：判据实现一次放进 `t004_common.py`，接口上**不接受调用方传入实测结果**
  （想把 status 写成字面量在这个接口下做不到）；平台提交客户端放进 `oj_submit.py`，
  口令只走环境变量、不入库。后续批次**从 `build_t004_round4.py` 派生**，不再从零重写。

- 2026-07-26 · **人拍板：给本地判题器加 PyPy3（红线 1 签字），单开 T-008**（出处：T-008、
  `tests/test_judge.py` 的 `SandboxContractTests` 与 `PyPy3Tests`）。起因：平台有 PyPy3 档，
  3728/4009 的 Python 参考解法在 Python3 档 TLE、PyPy3 档 Accepted，而本地 `judge.py` 只支持
  python3/c/cpp —— **同一份算法正确的 Python 解法平台能过、本地判 TLE，本地镜像比真实平台更严**。
  人已在本机安装 pypy3。**落地口径：沙箱限制一行未动** —— PyPy3 与 CPython 共用同一个
  `_limits()`（CPU 4s / FSIZE 2MB / AS 768MB）和同一个 5 秒墙钟；实测 PyPy 在现有限制下就能跑，
  **没有为它放宽任何一项**。这一条此后由 `SandboxContractTests` 机械保证，不再是交接里手写的一句话。

- 2026-07-26 · **人拍板：平台提交增加 PyPy3 一档**（出处：本轮 HANDOFF、
  `t004-round4-report.json` 的 `pypy3_check_2026_07_26`）。做法：Python3 档 TLE 的，
  再用 PyPy3 提交一次再下结论。**实证很直接**：3728 与 4009 的 `samplecode.py` 在 Python3 档 TLE，
  换 PyPy3 双双 Accepted（3094ms / 389ms）——**先前「这 2 题没有 AC 的 Python 参考实现」的结论是错的**，
  算法本身够，只是 CPython 跑不进时限。反过来，8 份 TLE/MLE 的对拍 oracle 换 PyPy3 后**一份都没救回来**，
  确认它们是算法层面的暴力、只适用于小规模生成数据。
  **顺带记一个缺口**：本地 `judge.py` 支持 python3/c/cpp、**不支持 pypy3**，本机也未装。
  于是同一份 Python 参考解法平台 PyPy3 能过、本地判 TLE——就这类题而言本地镜像比真实平台更严。
  是否给本地判题器加 PyPy3 属**判题沙箱改动（红线 1），留待人拍板**。

- 2026-07-26 · **工作规范：所有 `_made` 数据目录及其内容必须纳入 Git**。包括当前已有目录和今后
  新生成的目录；交付生成数据时必须与对应报告、索引变更一起提交并 SSH 推送，不能只提交生成器
  或依赖工作区临时重建。`.gitignore` 仅排除抓取的真实测试数据，已明确豁免
  `data/openjudge/tests/*/*_made/` 及其全部内容。

- 2026-07-26 · **人拍板：Python 参考解法在平台超时就写 C++**（出处：同上）。
  已落地：`03728_made/samplecode_ac.cpp`（Accepted 17ms）、`04009_made/samplecode_ac.cpp`（Accepted 40ms）。
  `samplecode.py` 保留，用于本地生成数据与对拍；平台背书的参考实现以 `samplecode_ac.cpp` 为准。

- 2026-07-25 · **人拍板：`4102`/`18250`/`20741`/`24729` 这 4 份人手写的生成器维持原样，不加种子、
  不重新生成数据**（出处：T-007 回归扫描报告 `regression-sweep-2026-07-25-report.json`、
  本轮 HANDOFF）。事实：这 4 份出自 commit `346c5aa`，`producecase.py` 用的是无种子
  `random`，因此不满足 2026-07-25 立的「重跑 `producecase.py` 后 `data/` 必须逐字节不变」。
  数据本身经 samplecode 逐组复算全部自洽，质量没有问题，只是丢了「生成器 → 数据」的
  可追溯性。理由：加种子就意味着替换掉人自己产出的数据，代价大于收益。
  **落地口径**：B1 字节复现记为 242/246，这 4 题作为**已知且已接受的例外**，
  后续回归扫描跳过它们的 B1 判据，不再计为缺陷。

- 2026-07-25 · **人拍板：26573 不放宽 n 的范围，维持 0..8 共 9 组**（出处：本轮 HANDOFF、
  `scripts/build_t003_002_round3.py` 的 `g26573`）。理由：题面未给 n 上界，输出长度为 3^n，
  判题 2MB 输出上限使 n<=13 才可判；放宽到 0..13 也只有 14 组、仍到不了「≥15 组」，
  却要多约 2.3MB 数据——纯粹是体积换深度，不划算。该题的「去重 9 组 < 15」在报告里
  按**数据体积取舍**记录（`constraints` + `test_cases_note`），**不套用「输入域本身小于 15」的豁免**。

- 2026-07-24 · 协作脚手架落地，流程与文件职责沿用 Redmoon/collab 的形状；
  初始仲裁闸门暂为 `--verify` + 手动冒烟，T-001 完成后由 `tests/` 回归套件接管，
  `--verify` 作为统一入口委托运行测试并继续做语法检查（出处：`tools/handoff.py`）。
- 2026-07-24 · T-001 已决：判题器对 Python 源码先做语法编译，语法错误返回 CE；
  CPU 限额触发的 `SIGXCPU`/`SIGKILL` 返回 TLE；服务端对 URL 解码后的 `..` 路径
  直接返回 404，并支持 `CS101_DB` 注入隔离测试数据库（出处：`judge.py`、`server.py`、
  `tests/test_judge.py`、`tests/test_server.py`）。
- 2026-07-25 · 人拍板去掉 `producecase.py` 的重复存储（出处：`scripts/slim_producecase.py`、
  本轮 HANDOFF）：改回人的模版形状——**生成器 + 固定种子重新生成，不内嵌 CASES**
  （原来同一份输入在仓库里存两份，2.23MB）。全 80 题改写后 `producecase.py`
  2.23MB → 0.37MB，入库 `_made` 数据 10.8MB → 8.92MB。
  交付前自检新增一项：**重跑 `producecase.py` 后 `data/` 必须逐字节不变**。
- 2026-07-25 · 001d 复核后追加第五代自检项（出处：本轮 HANDOFF、`scripts/build_001d.py`）：
  ①**恒定输出探针** —— 把 20 组里频次最高的那份输出原样当解法提交，判 AC 即说明该题数据没有
  鉴别力（20123、6250 都是这个形状，此项可机械抓住）。②**报告自检字段必须是实测值**，
  不得写字面量常量；`constraints` 逐条打钩表是那个布尔唯一的证据，不得省。
  ③选生成范围前先看参考解法有没有 O(1) 捷径分支，范围要能把捷径和主算法都压到。
- 2026-07-25 · T-003 首批复核后追加第六代自检项：对题面定义为图/树/DAG 的生成器，必须在生成器内部用断言验证连通性、节点可达性、遍历序列同树、重边和无环等结构性质；报告只记录实际执行的结构检查，不用泛化布尔代替。
- 2026-07-25 · T-001 复核后补充两条测试纪律（出处：本轮 HANDOFF、`tests/fixtures/mirror/`）：
  ①**闸门必须在全新 `git clone` 上成立** —— 测试不得依赖 `.gitignore` 掉的抓取数据，
  判题夹具自带在 `tests/fixtures/mirror/` 并入库；确需走真判题器时只用 `*_made/` 题号。
  ②**新写的断言要做一次变异自检** —— 把被测那行改坏，确认对应用例真的失败，
  防止「删掉代码测试照样绿」的假覆盖。
- 2026-07-24 · 人拍板 T-002：项目本意是把 cs101.openjudge.cn 现代化，提供完整的
  编写、提交、反馈错在哪组数据的闭环；缺数据的题目用 LLM 生成测试数据，
  目录命名 `<题号>_made`（如 `4102_made`）以标记出处。分工：Codex 生成、Claude 复核。
- 2026-07-24 ·（已决）人的模版落地：`data/openjudge/producecase_prompt/` 37 份单题
  prompt（题面 + producecase_template.py + ac.py → LLM 产出 producecase.py），实物
  样板 `tests/4000-8210/4102_made/`。**以人的工作流为准**，Claude 起草的
  `tools/make_data_template.py` 已删；其样例自检 + 固定种子两条纪律并入 T-002 规格。
- 2026-07-24 · Claude 勘查发现并记录：① 恢复的 tests 树缺 `20000-29982` 桶
  （2236 条 catalog 引用悬空，判题会崩），等人补齐；② 人已生成的 `4102_made`/
  `18250_made` 因数据在 `data/` 子目录而未被索引器收录（`test_count=0`），
  修索引器纳入 T-002 范围。
- 2026-07-24 · 人提供两份人工验证题解合集作为参考解法来源（本机路径见 NOTES-claude
  2026-07-24 放量批条）：覆盖当前 499 个缺数据题号中的 **211 个**。规则：有题解的题
  samplecode.py 必须取自题解（样例断言仍强制），无题解的 288 个题 LLM 自写、排后。
- 2026-07-24 · ~~待人拍板~~ **人已拍板：生成数据入 git（选项 A）**。原问题：`tests/`
  整体在 `.gitignore`，catalog 提交后到重建前判题引用悬空、放量后每台机器都要
  「pull 后跑构建器」。落地：`.gitignore` 改为豁免 `tests/*/*_made/`（抓取的真实
  数据维持不入库），24 个 `_made` 目录（9.5MB 纯文本，含 producecase.py/
  samplecode.py/data）全部入库，仓库自洽。此后 Codex 交付放量批时数据直接随
  commit 走，不再有「需在服务机重建」的间隙。
