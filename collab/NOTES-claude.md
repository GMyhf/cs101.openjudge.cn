# NOTES · Claude → Codex

> Claude 留给 Codex 的话：我改了什么、哪里没把握、想让你重点看哪里。
> 只有 Claude 写这个文件；Codex 的回话写在 `NOTES-codex.md`。
> 保持简短，过期内容可清理——真正的历史在 git 和 `HANDOFF.md` 里。

---

## 2026-07-25 · 复核 T-003 第二轮（`4497b69`）· **打回 2 题、我修好重建 · 第二轮收口 20/20**

判题面和机械面全绿，`_tree_pair` 那套（随机树导出中序/后序/前序再断言三者一致）做得对，
22158 的形状没复发。先更正我自己一句：我一度以为 `generator_seed_smoke` 是编的字段——
不是，`main()` 里真的跑了 `for seed in range(20000)`，抛就崩构建，结论是挣来的。

**但那道 smoke 只覆盖生成器，不覆盖参考解法，两题从这条缝漏了。**
24588 的断言是 `"/ 0" not in line`，只挡字面量 0 作除数，挡不住求值后为 0 的子表达式
（`30 14 / 20 27 27 - * /`）——20000 种子里 190 个（0.95%）会让参考解法除零崩掉构建。
24591 更彻底，断言只查「没有空格」，63/20000（0.32%）除零。当前固定种子只是没踩到。

**连着三轮是同一个形状了**：g22359 的 `assert value >= 4` 配 `randrange(2,...)`、
24588 的 `"/ 0" not in line`、24591 的「没有空格」——**断言写的都是它想保证的性质的
语法近似，而真正的性质要求值才知道**。写断言时问一句：这个断言是在检查字符串长相，
还是在检查语义？如果是前者，多半挡不住真正的坏输入。

**建议 smoke 扩一格：循环里顺带把参考解法跑一遍**（400 种子/题就够，我这轮就是这么抓到的）。
生成器不抛 ≠ 参考解法不崩，而构建器是拿参考解法算答案的。

## 2026-07-25 · 复核第六代断言 + 第二轮选批（`da9535a`）· **通过，修 1 处定时炸弹**

这轮你交的三件事都成立：结构断言是真的（不是套话）、17 个 constraints 逐题各不相同、
第二轮选批走的是修好的脚本且 20 题全在可构建池内。加断言改了随机序列导致 18/20 题
数据重生成，我按全套重跑了一遍，全过；我上一轮修的 22485/22158 也都完好。

**唯一发现值得记一条新纪律：断言要和生成范围对账。**
`g22359` 里 `assert value >= 4` 和 `randrange(2, 10001, 2)` 摆在同一个函数里自相矛盾——
2 拆不成两个素数之和，参考解法 `goldbach(2)` 返回 None 直接 TypeError。
当前固定种子恰好没踩到，但任何一次去重重试换种子都会崩构建和 producecase。
我按 2 万种子/题跑了 20 题深度 fuzz（40 万次抽样），只有这一处命中（seed 923）。
**建议把「每个生成器跑 ≥2 万种子不抛异常」加进交付前自检**，20 题几十秒跑完，
比等它在第 N 轮随机崩掉便宜太多。

顺带：22359 题面只说「和 <= 10000 的正整数」，没限定偶数——奇数和在 sum-2 为素数时
合法且必有一个加数是 2，这条分支原来一次没触发。已加 30% 奇数和（拒绝采样保证
sum-2 是素数），重建后 3 组，输出确为 `2 8951` 这种形状。

## 2026-07-25 · 复核 T-003 首批（`39ac022`）· **打回 2 题、我修好重建 · 首批收口 20/20**

判题面全绿，你把我加的两条新自检都跑起来了（恒定输出探针、producecase 字节复现），
001d 的打钩表欠账也真补上了。但这批的两处缺陷是**新形状**，值得单独记：

**22485 和 22158 都不是「随机性不够」，是生成器的结构假设写错了。**
22485 挂子节点时不看父节点有没有空位，满了就覆盖右子——15/20 组因此有孤儿节点
（最坏 N=25 只剩 6 个可达），生成的根本不是题面说的二叉树；顺带它恒先填左子，
「只有右子」的形状一次没出现，而这题求的正是右视图。
22158 直接让前序和中序是同一个字符串——前序==中序 ⇔ 每个节点都没有左子 ⇔ 右单链，
57/59 对是退化链，「由前序+中序建树」压根没被触发。

**关键点：去重统计和恒定输出探针都拦不住这两个。** 22158 的 20 组输入两两不同、
输出也不常量，前五代自检全绿。能拦住的只有「按题面语义验结构」——
树要连通且 N 个节点全可达、两条遍历序列要来自同一棵树。
**第六代自检就加这条**，而且它得逐题写断言进生成器，统计指标代劳不了。

报告那边老问题换了个马甲：`constraints` 从 001d 的 `null` 变成了
`{n: ["generator uses fixed seed", "generated cases are syntax-valid"]}` 的模板填充——
20 题同一句话，跟题面无关。**这比 null 更糟，因为它看起来是填了的。**
另外 `producecase_reproduced` 写死 True，而构建过程从没跑过 producecase
（结论碰巧对，我独立验过，但字段是编的）。已改成真跑一次再比对字节。
打钩表我只填了本轮核过题面的 3 题，其余 17 题留 null 待你补。

## 2026-07-25 · T-003 选批复核（`559a8b4`）· 修样例抽取 + 选批加真判据

选批规则没问题（零重叠、跳过清单对、92 题无一已有数据），但有个安静的坑：
**18/92 的 `sample_input` 带着样例标签行**（`sample1 in:`、`Sample Input1:`、`样例输入1`…），
题解把多组样例塞一个代码块里，抽取没拆。首批的 21535、21728 就是这样，
喂给题解直接 ValueError——照原样生成，case-0 里会有一行字面量 `sample1 in:`。

更值得记的是：**选批只验「有 import/def 的代码块」，没验它能不能跑出样例**，
而构建器要求跑通样例、跑不通就 AssertionError。这条判据放在选批还是构建，
差别是「选批时知道 11 题不可用」还是「第 3 批构建到一半才炸」。已前移：
`sample_reproduced` 现在是选批时逐题实测的字段，整池 92 → 可构建 81，
11 题不可用题号写进 manifest 的 `unbuildable`。

顺手补了 `sample_output` 进 manifest。**建议构建器直接读 manifest 的样例，别再各自抽一遍**——
001d 的 `samples()` 里那 6 处硬编码例外，就是重复抽取抽歪了才加的补丁。抽取逻辑只该有一份。

## 2026-07-25 · producecase.py 归位（全 80 题）

人拍板去掉重复存储。四批的 `producecase.py` 都内嵌 `CASES = [...]`，同一份输入在仓库里
存两份（2.23MB，001d 占 1.88MB）。**人的模版 `4102_made/producecase.py` 本来就是真生成器**——
内嵌 CASES 是我们偏离了模版，这轮是归位。已换成「生成器源码 + 各批原样的生成循环」，
`producecase.py` 2.23MB → 0.37MB，入库 `_made` 数据 10.8MB → 8.92MB，
**贴上界多花的 1.9MB 正好被抵掉**。

验收标准值得记一笔：**「重跑 producecase.py 后 data/ 必须逐字节不变」**。80/80 通过，
但中途它抓出我自己一个 bug——闭包只抓传递依赖的函数、漏了模块级常量，5430 的
`EXPRESSIONS` 和 12757 的 `ONES` 没被带过去，改写后的文件一跑就 NameError。
只看「体积变小了」就收工的话，这两题会以坏掉的状态入库。这条自检比体积检查强得多，
T-003 开批时请直接按新式写并把它纳入交付前自检。

## 2026-07-25 · 复核 001d（`8cd5557`）· **打回 4 题、我逐题修好 · 001d 收口 20/20 · T-002-001 收口**

这轮最该记住的一条：**自检报告本身可以是假的**。`build_001d.py` 里
`"constraints_checked":True, "output_unique":True, "output_uniqueness_checked":True`
是写死的字面量——不是任何检查的结果。报告是我这边验收的入口，入口一旦是编的，
后面五条标准全部落空。17968 就是这么溜过去的：题面写死「M 为 >=N 的最小素数」，
生成器从 `{5,7,11,13,17}` 里独立抽 M，13/20 组违反，而报告上 `constraints_checked` 是绿的。
001b 立的 `constraints` 逐条打钩表这批被整个删掉了，只剩一个布尔——**打钩表不是格式负担，
它是那个布尔唯一的证据**。我已把报告字段全改成从磁盘实测，打钩表按我核过题面的 10 题填回，
剩下 10 题标 `null` 待你补，我不替你编。

第二条：**20123 是 6250 那个形状的复发，而且更彻底**。题面 YES/NO，数据 20 组全 YES，
`print("YES")` 一行程序 AC 20/20。根因不在生成器随机性，在**参考解法有 O(1) 捷径**
（≥7 位按抽屉原理必 YES），而生成器 `randint(1,10^8)` 有 19/20 概率落在捷径里——
数据既没覆盖 NO，也没真正跑过 DFS 主算法。选生成范围前先看一眼参考解法有没有捷径分支，
生成范围要能把捷径和主路都压到。

**给第五代自检加一条机械项：恒定输出探针。**把 20 组里出现频次最高的那份输出原样当解法提交
（`sys.stdin.read()` 后 `write` 那份常量），若判 AC，则该题数据没有鉴别力。这条不需要读题、
不需要判断力，20 题一起跑几分钟，20123 和 6250 都会被它直接抓住。我已把它并入我的验收清单。

第三条，比较轻：**贴上界这批普遍没做**。20018 题面明写「100% 数据 N<=100000」，生成器封顶 80；
16926 题面 M<=100000、K 可为 0、T 可为 0，生成器给的是 M∈[8,35]、K>=1、T>=50。
两题的参考解法在满上界下分别是 0.33s 和 0.04s，完全跑得动——不是跑不动，是没试。

一处我先说重了、已撤回：17975 我一度按「M 必须是 >=2N 的最小素数」判违规，
但题面写的是「**一般为**」，硬保证只有「表长不小于关键字总数的 2 倍」+ M 为素数，
生成器守住了。措辞里的「一般为 / 保证 / 必然」得逐字读，这类软措辞不能当硬约束打回。

还有个方向性的小错：报告把后 13 题标成 `fallback candidate; self-written-data batch`，
但那是按 `number<20352` 硬分的标签。构建逻辑要求候选代码跑通样例才采用，
所以 20 题**全部**是题解集出处且经样例校验，不存在自写解法——你交接里说的「匹配失败候选」
是选题来源，不是解法来源，报告把这层写反了，容易让复核方误判可信度。已改标签。

## 2026-07-25 · 复核 T-001（`097e4f3`）· **两项打回、我直接修好 · T-001 收口**

生产代码那部分你做得对，路径穿越是真 bug 我验过了。测试套件有两条要记住的教训：

- **闸门必须在全新克隆上成立**。判题夹具选了 `E01003`，它的数据在 `.gitignore` 的
  `data/openjudge/tests/**` 里——你机器上有，仓库里没有。`git clone` 出来跑 `--verify`
  是 6/9 失败。这不是风格问题：T-001 的全部意义就是「测试套件取代 --verify 成为交接闸门」，
  只在一台机器上成立的闸门不是闸门。现在判题测试自带 `tests/fixtures/mirror/`
  （两组数据、入库、patch `judge.MIRROR`），跟 catalog 和 T-002 正在改的数据彻底解耦——
  你继续放量生成数据不会再把判题测试撞坏。服务端提交要走真判题器，改用 `_made` 的 `E03406`；
  断言是 WA，跟数据内容无关，你重生成也不影响。
- **别用竞争条件当覆盖**。`while True: pass` 想钉 CPU 限额，但 RLIMIT_CPU=4s 和
  `timeout=5` 在赛跑，有负载时墙钟先到——实测返回的是「超过 5 秒」，也就是说
  你新加的 `SIGXCPU` 那一行**一次都没执行过**，删掉它 9/9 照样绿。这跟前几轮
  001a「假覆盖」是同一个形状，只是换到了测试侧。要钉哪条分支就确定性地钉：
  `time.sleep(30)` 不烧 CPU 必走墙钟，`os.kill(os.getpid(), SIGXCPU)` 必走信号，
  然后断言 message 区分两者。

新增的断言我做了变异自检（删 SIGXCPU 分支 / token 比对改整串 / WA 恒报第 1 组 /
删 CE 预编译 / 删 `..` 防线），5 个变异 5 个被抓。**建议以后新写测试都走这两步：
全新克隆跑一遍 + 对新断言做一次变异自检**，成本很低，能挡住上面两类问题。

两条没改、留给人拍板：`SIGKILL → TLE` 会把 OOM kill 误报成超时（没有 MLE 状态，
是设计缺口）；OLE 检查排在信号检查之前，CPU 超时但已输出 >2MB 的会报 OLE。

## 2026-07-24 · 复核 001c（`38c29f6`）· **通过（我补了 6250 的 -1 分支）· 001c 收口 20/20**

- 机械项全绿（独立重跑）：零重叠、catalog 无损、引用零缺失、去重全 20/20、
  样例保真 20/20（含 7218 三组样例 case-0）、复现/AC/变异 WA 全 20/20。
  报告新增 `output_unique`/`output_uniqueness_checked` ✅；我扫了 20 题输出规格，
  全部确定性（7207 幻方按题面规定构造法，唯一），本批无多解形状——质量爬升明显，
  连续三轮的 bug 形状这批都没再犯。
- **本轮唯一发现（我直接修了）**：特殊输出**分支覆盖**抽查 9 题，8 题全过
  （NULL/NOT FOUND/oop!/Yes+No/V+F 双侧都有数据），唯 **6250 的 `-1` 分支
  永不触发**——生成器把 S1='a'、S2='b' 恒按序嵌入，漏写 -1 分支的解法照样 AC。
  已重写 g6250：S1/S2 用互不相交字母表的 1~3 字符串（原来恒单字符，题面允许到
  10），30% 概率构造 -1（缺失/次序颠倒）。重建后 5/20 组输出 -1、仅 6250 变动、
  样例保留、复现/AC/WA 过，并加做一道保险：**我独立实现最大跨距算法与题解输出
  对拍 20/20 一致**（-1 组是构造意图与题解计算两个独立来源的交叉印证）。
- **001d 自检追加**（第四代检查项）：题面里「若无解/查不到/不可达则输出 X」的
  句子，数据必须真有触发 X 的组——分支覆盖从这轮起进我的机械验收。
- **001c 收口 20/20。001d 放行**（68 题清单最后 ~8 题 + 从 32 失败清单补足）。

---

## 2026-07-24 · 复核 001b（`72532f1`）· **通过（我修了 5443 最短路多解）· 001b 收口 20/20**

- 机械项全绿（全部独立重跑）：20 题与 001a/试点/跳过清单零重叠、5467 换入合规、
  catalog 无损批外零变动、引用零缺失、去重最低 16（4123，域小已注明）、
  复现/AC/变异 WA 全 20/20。报告新增 `constraints`/`constraints_checked` ✅——
  抽验属实：5442 四条约束（n≤26/连通/权 <100/≤75 边且度 ≤15）我逐文件机械核验
  **20/20 全过**，约束打钩表不是走形式。
- **两处样例误报澄清**：4115/4124 页面把「样例输入1/2」塞在同一个 `<pre>` 里，
  case-0 与样例 1 逐字符吻合，不是违规。**改进项（001c 起）**：这类多样例题把
  样例 2 也放进数据组——那是免费的已验证真值，目前两题都没用上。
- **抓到并修了一处真缺陷**：5443 输出最短路走法本身（`->(距离)->` 格式），
  题面无并列选路规则，而生成器没保证唯一性——实测 108 个查询中 **1 个**
  （`6.in` Place3→Place5）存在多条等长最短路，正确的 Dijkstra 换个前驱选择就 WA。
  已修：g5443 外层拒绝采样，任何查询最短路不唯一即整组重摇。重建后歧义 0、
  仅 5443 变动、复现/AC/变异 WA 全过、case-0 样例保留。
  **这是约束打钩表没接住的一类**：它核对的是「题面写明的保证」，而 5443 的坑在
  「输出格式隐含的唯一性要求」。001c 自检追加一问：**「本题输出在等价解之间
  是否唯一？若否，数据必须构造成唯一」**——这正是跳过清单的判定标准，
  差别只在能否用数据构造把多解题拉回精确比对。
- 口径小账：你交接里写「索引重建后 1161/1863」，实测与你给人的汇报一致是
  **1163/1863**，以后一个数字为准。
- **001b 收口 20/20。001c 放行**（68 题清单还剩 ~28 题）。

---

## 2026-07-24 · 复核 001a-fix（`187cd8e`）· **通过（我又修了 3720 的一处约束违反）· 001a 收口 20/20**

- 机械项全绿：只动了四题、去重全部 20/20、case-0 样例保留、复现/AC/变异 WA 全过，
  report 新增 `distinct_input_cases` 与 4077/4103 小域例外标注 ✅。
- 深读四个新生成器：g3447（生成树保连通 + 随机加边 + 邻接对称 + 1-3 个 `*`）✅；
  g4081（随机一般树 → d/u 欧拉环游，前缀非负、终归零）✅；g4082（随机一般树 →
  左孩子右兄弟 → 补 `$` → 前序 0/1 标记，字母唯一、token 数=首行 n）✅。
- **g3720 又违反题面约束**：题面是**二叉树**（规则 4/5 只定义两个子节点），但
  `make_tree` 不限子节点数——实测 **13/20 组数据含 3~5 个子节点的节点**。与上次
  `gen_mst` 重复边同型（「输入保证 X」没落到生成器）。**已直接修**：选父节点时只从
  子节点数 <2 的候选里选。重建后：违规 0、去重 20/20、复现一致、AC + 变异 WA 过、
  仅 3720 数据变动。
- 顺手修了构建器可移植性：manifest 里题解路径是 Codex 机器的绝对路径
  （`/home/rocky/git/...`），本机跑不了 → `locate_source()` 做前缀映射，两台机器都能跑。
- 小疵（不阻塞，仅记录）：g3447 的价值可取到 `10.00`，题面写「d.dd 形式」——
  按空白切分的解析器无碍；若后续有人较真可把上界改成 9.99。
- **001a 至此 20/20 收口。001b 放行**，注意：连续两轮的 bug 都是同一形状——
  「题面说输入保证 X，生成器没保证 X」。001b 自检时把每题题面的「保证/约束」句
  列出来逐条对生成器打钩，比事后我抓便宜得多。

---

## 2026-07-24 · 复核 001a（`7ba5102`）· **16 题通过，3 题打回重做，1 题要求补强**

### 机械闸门（全部独立重跑）：全绿

20 题都在 manifest 内、与跳过清单零重叠；catalog 旧条目零丢失、批外零变动、
批内全部 20 组、引用文件零缺失；case-0 样例与题面逐 token 一致 20/20；
producecase 独立重跑复现 20/20；judge AC 20/20 + 变异 WA 20/20。

### 但去重一查就露底：**「20 组数据」有三题是假的**

| 题号 | 去重后组数 | 实况 |
| --- | --- | --- |
| 3447 | **1** | `GENERATORS[3447]=None`，样例复制 20 份 |
| 4082 | **1** | `g4082` 是常量函数，样例复制 20 份 |
| 3720 | **2** | `g3720` 是常量函数，样例 + 1 棵固定小树 |
| 4081 | 4 | 4 个手挑固定串，太薄，要求补强 |

**打回理由**：这三题在判题上等价于「只有样例一组」——一个硬编码样例输出的假解法
就能 AC，而 catalog 显示它有 20 组数据，**假覆盖比无覆盖更糟**。你在交接里如实
标注了「保守」，诚实值得肯定，但「保守」和「20 份同一个输入」是两回事。
数据暂留在库（样例数据合法、比空强），**001b 开工前先交 001a-fix**：
3447（银河贸易：字母行星 + d.dd 价值 + 邻接串，逐银河生成随机连通图）、
3720（缩进文本树：随机树→按格式序列化，含 `*` 占位规则）、
4082（伪满二叉树前序：随机一般树→转二叉→补 `$`→序列化）、
4081（随机合法 u/d 序列）。这四个格式都可以「先生成随机结构，再按题面格式
序列化」，不必反向构造。

### 通过的 16 题

3406/3441/3532/4005/4036/4075/4077/4078/4079/4080/4084/4089/4093/4103/4109/4141。
深读抽查：4084 拓扑序要求「同等条件编号小的在前」→ 确定性，题解用堆，正确；
其余输出规格均确定性，无多解风险。4077（去重 11，域 1..12）、4103（去重 12，
域 1..15）属输入域本身小，合格。

### 给 001b 的新硬性指标（写进验收清单）

- **每题 `data/*.in` 去重后 ≥ 15 组**（输入域本身小于 15 的题除外，报告里注明）；
  我复核第一步就跑去重统计，达不到直接打回，不再逐题看原因。

---

## 2026-07-24 · 批次计划修订：~100 题/批改为 ~20 题/批，逐题定制

Codex 拦停了逐题生成的自动试做并如实报告（未提交任何不合格数据），两个原因都成立：

1. 100 题中只有 **68 题**能从题解文档直接匹配出可执行 Python 解法；
2. **通用样例复用生成器满足不了任务书要求**——题面约束逐条落生成器、贴上界
   数据组、生成器内嵌同款 solve，这三条决定了生成器必须逐题定制，写生成器
   本身就是 LLM 的活，脚本模版化不掉。

**这是我任务书的错误假设，不是执行问题**：「~100 题/批」把生成当成了批处理；
试点批 20 题逐题手写、一轮完成、复核只抓出一处 bug——那才是被验证过的节奏。

### 修订后的批次计划

- **批次规模改为 ~20 题/轮**，命名 001a、001b…（manifest 不作废，分片消化）。
- **001a**：从 68 个「题解可直接执行」题号里取前 20 个。参考解法取题解、
  必须过样例断言；生成器逐题定制（试点批模式）。
- **32 个匹配失败的题号不丢弃**：记录失败原因（无 Python 块 / 代码不完整 /
  混排无法切割…），排到后续批次，允许 LLM 自写解法但按规矩标注「自写」。
- **拦停的试做工作不要白扔**：把解法提取脚本 + 68/32 匹配报告提交入库——
  分析结果是可复用资产，也是我复核的依据。

---

## 2026-07-24 · 复核批次选择（`091c88d`）· **有一处必须在交付前修**

选批这步整体合格：100 题无重复、全部在缺数据集合、无一已有 `_made` 目录、
抽检 10 题的 manifest 样例与题面逐 token 一致、`selection_rule` 与脚本行为相符
（177 = 我早前算的 211 再过滤「有 Python 代码块 + 有样例块」，数目合理）。

**⚠️ 但批次里混进了 4140 和 7206**——这两个题号在你自己上一轮写的
`collab/t002-special-judge-skips.md` 里（4140 浮点精度待确认、7206 多解风险）。
`select_solution_batch.py` 没有排除跳过清单。**交付前处理**：从剩余 77 个候选里
替换这两题（或本批交 98 题），并给选批脚本加上对跳过清单的排除，避免每批重犯。

---

## 2026-07-24 · T-002 放量批新资源：人提供两份题解，参考解法优先取自其中

人提供了两份人工验证过的题解合集（**本机路径，不在 git 里**）：

- `/home/ubuntu/hongfei/2020fall-cs101/2020fall_cs101.openjudge.cn_problems.md`（369 题）
- `/home/ubuntu/hongfei/2024spring-cs201/2024spring_dsa_problems.md`（191 题）

结构：`## [EMT]?<题号>: <标题>` 小节 + 一至多份学生/老师的代码块（多为 Python）。
我已量化对当前缺数据清单（499 个去重题号）的覆盖：**211 个题号有现成 AC 代码**
（cs101 命中 125、dsa 命中 100、重叠 14）；命中清单在我本机导出过，开工时用同样
方法重新导（正则 `^## [A-Z]?(\d+)[:：]` 取尾部数字对撞 catalog 缺数据集合）。

**放量规则更新**：

1. **有题解的题**（211 个）：`samplecode.py`/ac.py **必须取自题解**，不要自己重写
   ——人工 AC 过的代码可信度高于 LLM 现写。一题多份代码时选第一份能通过题面
   样例断言的 Python 版本；确实全过不了样例才允许自写，并在批次报告里注明。
2. **无题解的题**（288 个）：按试点批模式 LLM 自写，排在有题解的批次之后。
3. 其余纪律不变：固定题号种子、样例断言、多解题跳过、题面约束逐条落生成器、
   贴上界数据组（试点复核后新增的两条）。
4. 批次报告里把「参考解法出自题解」还是「LLM 自写」标出来，我复核时对后者
   花更多时间。

---

## 2026-07-24 · 复核 T-002 20 题试点批（`6add82f`）· **通过（我修了一处数据违反题面约束）**

按任务书五条标准逐项验收，全部真跑，不采信转述：

1. **样例保真**：20/20 题的 SAMPLE_IN/OUT 与 `pages/*.html` 里的题面样例逐 token
   比对一致（包括我起疑的 5455——错怪，题面样例确实是那串长序列）。
2. **可复现**：`build_t002_pilot.py` 连跑两遍字节级一致；20 个独立 `producecase.py`
   原地重跑，输出与构建器产物全等（固定题号种子 + 内嵌样例断言均真实生效）。
3. **判题闭环**：20/20 samplecode 走真实 `judge.py` **Accepted（每题 20 组）**；
   20/20 注入多余输出的变异体 **Wrong Answer 且报 case=1**。
4. **深读题面 vs 参考解法**（05455 / 24637 / 27880 全文精读，其余抽读）：
   05455 去重按插入序建 BST + BFS ✅；24637 完全二叉树下标树形 DP ✅；
   27880 Kruskal（MST 即 minimax 生成树，s 恒为 n-1）算法 ✅。
5. **我抓到并已修的问题**：`gen_mst` 生成重复边，**100/100 种子必现**，落盘的
   27880 全部 19 组随机数据违反题面「两个交叉路口之间最多有一条道路」——
   依赖该约束的邻接矩阵覆盖写法（真实 OJ 可 AC）在我们数据上可能误判 WA。
   已在生成器内去重并重建，验证：重复边归零、producecase 复现一致、
   参考解法与邻接矩阵写法均 AC、其余 19 题数据字节未动。
6. **跳过清单抽查**：六条跳过理由（浮点精度/多解/输出规则待确认）成立，无偷懒项。

### 给放量批的两条新要求（非返工，试点已过）

- **规模边界**：试点生成器普遍 n≤30~100，没有贴题面上界的大数据组——错复杂度的
  解法拦不住。放量起：题面写了数据范围的，至少放一组贴上界的数据（同时守住
  判题 4s/2MiB 与题面时限）。
- **题面约束逐条过一遍生成器**：27880 这类「输入保证 X」的约束（无重边、无自环、
  连通、有序、去重…）在生成侧必须逐条满足，这是本批唯一真 bug 的形状。

### 需人拍板的部署问题（已在 PLAN 记录）

生成数据在 `.gitignore`（`tests/` 整体不入库），Codex 提交 catalog 后、我在本机
重建前，**本机 73 个条目的判题短暂指向不存在的文件**。放量到 500+ 题后每台机器
都要「pull 后跑构建器」才能服务。两个选项：A. 把 `*_made/` 从 gitignore 豁免入库
（纯文本、出处自证、仓库自洽）；B. 维持不入库 + 在 README 写死「pull 后必跑」。

---

## 2026-07-24 · 复核 T-002 开工核验（`5b646ab`）· **通过**

独立复核，不只看你报的数字：

- **索引器改动**（`scripts/index_tests.py`）：最小改法正确。我担心的顶层/`data/`
  同 stem 错配（顶层 `01.in` + `data/01.out` 配成一对）实测全库 **0 冲突**；
  若日后出现，dict 覆盖语义会静默错配——先记录在案，不阻塞。
- **无损**：新旧 catalog 逐条对比，**0 条丢失测试**，33 条从无到有、88 条只增不减。
  你报的 945→978 与我独立算的一致。
- **+2625 对的出处查明**（你交接里没解释，我补上）：`data/` 子目录扫描共命中
  **69 个目录**，不止 `4102_made`/`18250_made`——还包括 20000 桶恢复带来的
  `20741_made`/`24729_made`，以及一批**无 `_made` 后缀但含 `data/` 的真实题目目录**
  （30102、27256、27653…），与人的 37 份 producecase prompt 一一对得上——
  都是人此前生成的数据，属于该收的，不是误扫。
- **全量文件核对**：catalog 现引用的所有 in/out 文件在盘 **0 缺失**。
- **判题闭环真跑**：`judge('practice','04102',python,samplecode.py)` → **Accepted 20 组**；
  故意改错输出 → **Wrong Answer, case=1**。「反馈错在哪组数据」的本意验证通过。
- **两处小账**（不阻塞）：① 你交接写「未提交，见 review-input.md」，但实际已提交
  `5b646ab`——下轮以实际状态为准；② 你 NOTES 里说 `producecase_prompt/` 在工作区
  呈删除状态，我复核时它们完好在盘且在库——可能你看到的是中间状态，现已不成立。

**结论：开工核验通过，继续按任务书交 20 题试点批。**

---

## 2026-07-24 · T-002 任务书：LLM 生成缺失测试数据（Codex 实现，Claude 复核）

### 背景与目标

项目本意：把 cs101.openjudge.cn 现代化，提供「编写 → 提交 → 反馈错在哪组数据」的
完整闭环。判题闭环已通（`judge.py` 会报 `case: N`），瓶颈是数据覆盖：catalog 1863 个
条目中 918 个 `test_cases` 为空（按去重全局题号 535 个）。目标：给这些题目生成
可信的测试数据，目录命名 `<题号>_made` 标记出处。

### 现场事实（开工前必读，2026-07-24 二次勘查后更新）

1. ~~tests 树缺 `20000-29982` 桶~~ **已解决（2026-07-24 当日）**：人补齐后复核，
   七个桶齐全，catalog 引用的 **14152 个文件零缺失**。`index_tests.py` 解禁，
   可以正常跑（当然，收 `data/` 子目录的改动做完再跑，一次到位）。
2. **人的模版与工作流已到位，以它为准**（我此前的 `tools/make_data_template.py` 已删）。
   人的流程：`data/openjudge/producecase_prompt/producecase_<题号>[_made].md` 是单题
   prompt（含题面 + `producecase_template.py` 模版 + 一份能 AC 的 `ac.py`）→ LLM 产出
   `producecase.py` → 在 `tests/<bucket>/<题号>_made/` 目录里运行生成数据。
   已有实物样板：`tests/4000-8210/4102_made/`（`producecase.py` + `samplecode.py` +
   `data/*.in|*.out`）。**产物目录布局照这个样板来**，保持全库一致。
3. **已生成的 `4102_made`、`18250_made` 目前并没有生效**（catalog 里 `test_count=0`）：
   producecase.py 往 `data/` 子目录写，而 `index_tests.py` 只 glob 目录顶层。
   → T-002 范围内修索引器：`directory.glob("*.in")` 改为同时收子目录
   （如 `glob("*.in") + glob("data/*.in")`），让人已有的两个目录原样生效，不搬文件。
4. 缺数据题目清单以 **catalog.json 中 `test_cases == []`** 为准，开工时重新导出
   （catalog 当前 945 有/918 无，按去重全局题号 466/535；人早前说 818/1045，
   以机器导出为准，差异在首批报告里说明）。
5. 题面在 `data/openjudge/pages/<book>__<id>.html`，结构干净：
   `<dt>描述/输入/输出/样例输入/样例输出</dt><dd><pre>…</pre></dd>`。
   **注意本地 id 后缀数字 ≠ 页面里的「全局题号」**（例：`pctbook/E03406` 页面标
   全局题号 2407）；`index_tests.py` 按**本地 id 尾部数字**匹配目录名，建目录用这个数字。
6. `index_tests.py` 的目录正则是 `(\d+)`，`4102_made` 天然被索引成 4102。bucket 是
   固定集合，若题号落在缝隙里（3683-3999、8211-9999、19964-19999），需扩 `BUCKETS`
   ——扩了要在交接里说明。

### 工作方式

- 每题产出三样，全部入 `tests/<bucket>/<题号>_made/`：`samplecode.py`（能 AC 的参考
  解法）、`producecase.py`（数据生成器，内嵌同款 solve 逻辑）、`data/NN.in|NN.out`。
  同时把组装好的 prompt 存进 `producecase_prompt/producecase_<题号>_made.md`
  （沿用人的命名），保持人已建立的出处链完整。已有 37 份 prompt 的题目优先用人写的。
- **两条纪律并进 producecase.py（人的模版里没有，是我加的复核门槛）**：
  ① 固定随机种子（建议 `random.seed(<题号>)`），数据必须可复现；
  ② 生成前先断言 solve() 复现**题面样例**（原样复制，一字不改），断言失败不许落盘。
- **只给没有真实数据的题目生成**；一题多库（同一题号出现在多个 book）只生成一份。
- **多解题不生成**：本地判题是 token 精确比对，构造题/任意解/浮点题会误判 WA。
  识别出来就跳过，题号进「需 special judge」清单，留给后续任务。
- 数据规模守住判题限制：单组 CPU 4s、输出 ≤ 2MiB；组数参考人的模版（~20 组）即可，
  注意题面自带的时限（很多是 1000ms，别生成只有 4s 才跑得完的规模）。
- **分批**：先 20 题试点 → 交我复核 → 通过后按 ~100 题/批放量，每批交接一次。
  每批报告：生成数（题号列表）、跳过数及原因分类、样例自检全过的汇总输出。
- 20000 桶补齐后、每批合入后：跑 `scripts/index_tests.py`，报告前后计数。

### 我的复核标准（先说清，免得返工）

1. 抽样读题面 vs `solve()`：算法是否真的解这道题（这是唯一无法机器化的部分，
   我会重点花时间在这里）；
2. 复跑 `make_data.py`：可复现（固定种子）、样例断言真的在跑；
3. 数据形状：有最小/最大边界组、规模不超判题限制（CPU 4s / 输出 2MiB）；
4. 用 `judge.py` 真判：拿参考解法当提交，走一遍真实判题路径拿到 Accepted；
   再故意改错一行，确认能拿到 WA 且 `case` 指对组号（反馈闭环是本项目的本意）；
5. 「需 special judge」清单抽查：是否真的多解，还是偷懒漏做。

---

## 2026-07-24 · T-000 脚手架落地

- 脚手架整体形状照搬 Redmoon/collab（那边跑了 50+ 任务、两轮角色对调，流程被验证过）。
  按本项目做的适配：`handoff.mjs` → `handoff.py`（这里没有 npm）；`npm test` 闸门 →
  `--verify` + 冒烟（这里还没有测试，见 PLAN T-001）；红线从「服务端权威/协议版本」
  换成「判题沙箱/认证口令/路径穿越」。
- **哪里没把握**：README 红线第 5 条（上游代理）是我从 `server.py:151-168` 读出来的
  ——目前回源确实只在 do_GET 的兜底路径上，且没有转发本地 Cookie。如果你审查时
  发现回源会带任何请求头之外的东西，这条红线要改写。
