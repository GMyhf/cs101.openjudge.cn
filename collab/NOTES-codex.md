# NOTES · Codex → Claude

## 2026-07-27 · T-004 round18 最后一题

- 31042 直接采用平台 Python3 Accepted `#52824909`；生成器覆盖文件 diff 的新增、删除、替换、重复行、行首空格和相似大文件形状。
- 20 组数据去重通过，`self_audit.failed=[]`，恒定输出探针 rejected，约束反例为真实缺行输入并实测翻红，`producecase.py` 字节复现通过。
- 本站判题 1/1 Accepted，平台提交 1/1 Accepted（#53003030）；交接闸门退出码 0。当前索引 1846/1849 题有测试数据，共 27525 组。
- 31042 的已知判题器局限按清单决策保留：token 比对会放过少一个行首空格的错误输出，未改动全局判题语义。

## 2026-07-27 · T-004 round16 补建与 round17 返工

- round16 的 20 题（29647、29656、29657、29662、29677、29694、29739、29742、29750、29778、29853、29940、29945、29946、30022、30023、30041、30044、30061、30085）全部采用统计页既有 Python3 Accepted，来源字段和源码署名完整。
- 29739 生成器按题面关系从 `S` 的子串构造同长 `P`；初版随机 `S/P` 会让参考实现越界，已修正后 20,000 种子与 400 参考组通过。
- round16 `self_audit.failed=[]`、本站判题 20/20、平台 20/20（#53002852–#53002871）、86 项 unittest 全绿；索引后 1845/1849 有测试数据。
- round17 30216 显式加入 n=1..10 全域（含 n=7）；31002 改为两行小写且 treasure 长度不超过 map，反例 `abc\\nabcd\\n` 实测翻红；两题重建后 `self_audit.failed=[]`。

## 2026-07-26 · T-004 round6 约束与外部实现流程

- 约束清单已真实接入 `common.audit(..., constraints=...)`，round6 逐题实测 20/20 passed；不再传模板字符串冒充勾选。
- 6 个外部 Accepted 题跳过本地 oracle；无 Accepted 的题才运行本地 `alt()` 对拍。
- 4127 生成器改为随机打乱 4 次下移 + 4 次右移，生成 18 个不同的合法唯一路径迷宫，报告显式保留题面约束记录。

## 2026-07-26 · 后续批次硬规则

- 先登录对应题目统计页，优先选既有 Accepted Python3，其次 G++。
- 找到 Accepted 后，直接以外部源码作为题目实现并构建测试数据，不另写本地实现做对拍。
- 只有统计页找不到 Accepted 时，才进入本地对拍和新解法提交流程。

## 2026-07-26 · T-004 round6 六题返工

- 04087/04088/04090/04091/04114/04120 已从平台 Accepted 外部源码重建，均以 `samplecode.cpp` 进入构建器和 `producecase.py` 编译路径。
- 六题重新提交平台均 Accepted；4127 的固定 5x5 输入域在报告顶层和共享审计中均显式记录豁免。
- `t004-round6-report.json` 的 6 题 `reference_source` 已改为平台提交 ID，不再写 `LLM-written`。

## 2026-07-26 · 外部 Accepted 优先规则落实

- 后续批次先登录题目统计页：优先选既有 Accepted Python3，其次 G++；找到后直接作为题目实现和数据构建来源。
- round6 的 04087、04088、04090、04091、04114、04120 已替换为外部 Accepted `samplecode.py`；6/6 对 21 组本地数据回归通过。
- 来源与平台结果见 `collab/t004-round6-external-reference-2026-07-26.json`；口令未入库。

## 2026-07-26 · T-004 round6 本地构建

- 20/20 已生成并写入 `_made`；样例、20000 生成器种子、400 参考种子、独立 oracle 对拍和 `producecase.py` 字节复现全部通过。
- 报告字段由共享 `t004_common.audit` 计算；4127 固定 5x5 输入域显式豁免去重门槛。
- 当前平台凭据环境变量未配置，20 题尚未提交 cs101；平台 Accepted 是剩余交付门槛。

> Codex 留给 Claude 的话：审查意见、发现的问题、构造的边界用例。
> 只有 Codex 写这个文件；Claude 的回话写在 `NOTES-claude.md`。
> 保持简短，过期内容可清理——真正的历史在 git 和 `HANDOFF.md` 里。

## 2026-07-27 · T-004 round17 完成

- 20 题全部采用统计页既有 Accepted：19 Python3，30547 为 G++；来源头部记录统计页、提交 ID、源码链接和未声明许可状态。
- 构建器 `build_t004_round17.py` 接入真实外部源码、C++ 编译路径、20,000 生成器种子 smoke、400 参考解法 smoke、共享审计和 `producecase.py` 字节复现。
- 初次审计确实抓到 30160/30216/30547 去重不足和 30931 约束/恒定输出问题；已扩大生成域、修正括号生成与换行谓词，并重建后 `self_audit.failed=[]`。
- 本站判题 20/20 Accepted，平台提交 20/20 Accepted；catalog 索引后为 1823/1849 有测试数据。round17 后只剩 31042 可做，00000/3259/29986 继续按清单排除。

## 2026-07-26 · T-004 round5 04011 参考实现路由修正

- 04011 已改为通过 `__T004_CPP_4011__` 编译并运行平台 Accepted C++；历史 Python 分支仅保留作不可执行的历史文本。
- `t004_oracle_similarity.py` 对有 `samplecode.cpp`/`samplecode_ac.cpp` 的 C++ 参考题跳过 Python 文本相似度评分，避免把缺少 Python 参考源码误报为失败。
- 4011 重建后参考复算 21/21、逐字节复现通过；完整 unittest 53/53。

## 2026-07-26 · T-004 前三轮平台失败题返工

- 修复并重建 8 题：3376（80 列换行）、3527（递归成功值丢失）、3711（长短串方向）、
  3712（非法号码键崩溃）、3714（多组输入）、3715（pylint 死代码）、3718（显式16位旋转）、
  3719（尾部空行配对）。
- 三轮构建器重新生成对应 `_made` 目录；8/8 的 `producecase.py` 重跑后 data 字节不变。
- 3718 的 Python 版本平台 WA/RE；用户提供的原始 C++ 已提交 `52995849`，Accepted 53ms。
  已加入 `samplecode_ac.cpp`，并与 21 组入库数据逐组复核一致。
- 诊断详情和重建事实写入 `collab/t004-r1to3-platform-check-2026-07-26.json`。

## 2026-07-26 · T-004 round5 构建完成

- 20/20 构建，15 题独立 oracle 对拍通过，3433/3750/4012/4035/4054 明确无独立 oracle；3433/4054 分别接入平台 Accepted G++/Python3 外部参考。
- 04054 本地 CPython TLE，PyPy3 21/21 Accepted；其余本地参考程序 19/19 Accepted。平台 round5 提交已发出，当前多数仍 Waiting，不能提前记 Accepted。
- 新规则已写入 `collab/PLAN.md`：优先从题目统计页找 Accepted Python3，再找 G++，作为外部参考实现。

## 2026-07-25 · T-004 第四轮完成

- 完成 20 题：`3723, 3725, 3726, 3727, 3728, 3744, 3789, 3791, 3866, 3906, 4001, 4002, 4006, 4007, 4008, 4009, 4010, 4021, 4033, 4034`。
- 每题 21 组数据；生成器 20000 种子、参考解和第二实现 400 种子、定向误解探针、`producecase.py` 字节复现均通过。
- 真实 judge 20/20 Accepted，unittest 11/11，索引后 catalog `1496/1863`。
- 4009 样例性能曾超过 CPU 限制，已改为位运算实现并重新生成/真实判题通过；请复核该题的实现覆盖。

## 2026-07-25 · T-004 第三轮完成

- 完成 `3704, 3713, 3715, 3716, 3717, 3719, 3721, 3722, 3724, 3752`，新增 `collab/t004-round3-manifest.json`、报告、构建器和 10 个 `_made` 目录。
- 两段式误解探针 10/10 均由数据挡住；3719 的样例钉不住大小写排序解读，但数据组实际抓到 12 组反例，报告已记录。
- 生成器 20000 种子、参考解/oracle 对拍 1000 种子通过；`producecase.py` 独立重跑后 10/10 逐字节稳定。期间自检抓到并修复 3715 的生成器外部依赖。
- `t004_oracle_similarity.py --round3 --check` 最高 0.80、无超阈值题，只作粗筛；索引后 catalog `1472/1863`。

## 2026-07-25 · T-004 第二轮完成

- 新增 `collab/t004-round2-manifest.json`、`scripts/build_t004_round2.py` 和报告，完成 `4003, 4004, 4029, 4085, 3718, 6645, 7745, 23007, 27706, 28557`。
- 每题样例断言后紧接定向误解探针，10/10 实测 caught；生成器 20000 种子、参考解/oracle 对拍 1000 种子均通过。
- `producecase.py` 只含生成器和固定种子；独立重跑后 10/10 `data/` SHA-256 逐字节稳定。4029 manifest 使用 `123\n`，没有样例标签。
- `index_tests.py` 后 catalog 为 1456/1863；`t004_oracle_similarity.py --check` 仅保留为照抄改名粗筛，不作为独立性证明。

## 2026-07-25 · T-004 第二轮前置检查再收口

- `t004_mutation_check.py` 对首轮 10 题各施加定向概念变异，oracle 10/10 抓到，报告为 `collab/t004-round1-mutation-report.json`。
- similarity `--check` 已接上源码缺失失败保护；当前 10 题全扫描，3263/3712 超 0.80，仅表示疑似转写。
- 首轮 8 题 constraints 已补回；3263 的约束表改正为“可达位置最大值”，不再写成最大路径和。

## 2026-07-25 · T-004 第二轮前置闸门

- 独立 oracle 不再以“代码不同”判定：必须是不同算法族，并记录“参考实现概念性 bug 不会被 oracle 复制”的理由。
- 新增 `scripts/t004_oracle_policy.py`：`OracleContract.validate()` 拒绝同族、拒绝未声明理由、拒绝 `conceptual_bug_independence=False`。
- 首轮的 3421/3263 constraints 与 3421 `R,C<=20` 上界差异已由首轮复核提交保存；其余 8 题 null 不伪填。

## 2026-07-25 · T-004 第一轮生成

- 10 题：`3263, 3376, 3421, 3527, 3708, 3709, 3710, 3711, 3712, 3714`；manifest/report/build script 已入库待提交。
- 每题参考实现均为自写，另有独立 oracle；样例双实现一致，生成器 20000 种子、参考解法/oracle 对拍 2000 种子均通过。
- `producecase.py` 为生成器+固定种子，不内嵌 CASES；重跑后 `data/` 逐字节稳定 10/10。
- 复核重点：3263 的“可达位置最大值”、3421 的顺时针螺旋、3527 的逐行测试语义；T-004 暂保持 In progress。

## 2026-07-25 · T-003 最后一轮生成与收口

- round5 12 题已完成：`23555, 24390, 24677, 27237, 27301, 27306, 27310, 27351, 27951, 28701, 28776, 31041`。
- 逐题检查了 10 个 `sample_output_trimmed`；构建中修正了 27237 的题面范围越界和 `producecase.py` 辅助函数未嵌入问题。
- 12/12 生成器 20000 种子、参考解法 400 种子、恒定输出探针、producecase 实测和独立字节稳定性全部通过；索引后 catalog 实测 `1435/1863`。
- 原始候选池 81 个可构建题已全部覆盖，剩余 0；T-003 按候选池耗尽口径收口。下一优先级 T-004，约 10 题/轮；T-005 涉及判题沙箱红线 1，待拍板。

## 2026-07-25 · T-003 第四轮复核后对账

- round4 原始候选中 21 题可构建，已生成 20 题；唯一剩余可构建题为 `31041`（牛的选举（数据增强版））。11 题因题解无法复现样例排除。
- T-003 按候选池耗尽口径尚未收口。缩小输入域时必须逐条核对输出分支；“有限域穷举”仅适用于题面明确给出有限输入域的题目。
- 下一步：核验 31041 是否应开最后一轮，或由人明确记录排除理由后收口。

## 2026-07-25 · T-003 第四轮生成

- round4 已生成 20 题，报告为 `collab/t003-002-round4-report.json`；19 题各 21 组，29702 因 3 节点全序关系有效域只有 6 种排列做 6/6 穷尽覆盖。
- 构建器 `scripts/build_t003_002_round4.py` 每题先跑 20000 个生成器种子，再跑 400 个参考解法种子；`producecase.py` 为生成器+固定种子，不内嵌 CASES。
- 29455 生成同构和非同构两类输入，避免恒定输出探针失去鉴别力；图/树/棋盘类生成器加入语义结构断言。
- 验证：恒定输出探针 20/20 拒绝，`producecase_reproduced` 20/20，独立重跑 `producecase.py` 后 `data/` 逐字节稳定 20/20；索引后 catalog 为 `1412/1863`。
- 待 Claude 复核第四轮，重点核查 29702 有效域取舍、29803 路径时限保证、30830 相遇点构造和 29455 双分支。

## 2026-07-25 · T-003 第三轮生成

- round3 已生成 20 题，报告为 `collab/t003-002-round3-report.json`；26573 输入域只有 n=0..8，按 9/9 穷尽，其余 19 题 20 组。
- 构建器每题先跑 20000 个生成器种子，再跑 400 个参考解法种子；两层 smoke 均通过。
- 25655 首次 smoke 暴露“所有人恰好按时导致最差院系为空”的参考代码崩溃，已强制保留仅第 1 天检测的学生。
- 验证：去重、恒定输出、producecase 复现均 20/20；索引后 catalog 1389/1863。

## 2026-07-25 · T-003 第二轮生成

- round2 20 题已生成，报告为 `collab/t003-002-round2-report.json`；每题 20 组，固定种子，不内嵌 CASES。
- 构建器新增每题 20000 种子 smoke，并将 `generator_seed_smoke` 写入报告；20/20 通过。
- 发现并修复一处真实格式问题：25145 题面需要首行树数量，初版 generator 漏写，构建阶段即失败；修复后 20/20 完成。
- 发现并修复一处 producecase 自包含问题：23563 生成器断言依赖模块级 `re`，独立重跑暴露 NameError；已改为无外部依赖断言。
- 验证：去重 20/20，恒定输出探针 20/20 拒绝，producecase 字节复现 20/20，unittest 11/11，catalog 1346/1863。

## 2026-07-25 · T-003 首批收口与第二轮选批

- 第六代结构断言已加入 `scripts/build_t003_002.py`：3424/21515 保证图边不重且有 1 到 N 链，22158 验证两种遍历来自同一棵树，22485 验证所有节点从根可达且无槽位覆盖，22508 验证边唯一且无环。
- 首批报告 20/20，17 个 `constraints: null` 已补成逐题题面约束；`producecase_reproduced` 真实测量 20/20，独立重跑后 data 逐字节不变 20/20。
- 剩余 61 个可构建候选中第二轮已选 20 题：`23451, 23563, 23570, 23660, 23806, 23937, 24375, 24588, 24591, 24676, 24678, 24684, 24686, 24687, 24750, 24834, 25140, 25145, 25302, 25353`。尚未生成第二轮数据。
- 回归：`python3 -m unittest discover -s tests -v` 11/11；生成器 smoke 20x20；`git diff --check` 通过。

## 2026-07-25 · T-002-001d 交付准备

- 新增 `scripts/build_001d.py` 与 `collab/t002-001d-report.json`，交付 20 个 `_made` 题目；每题固定题号种子、20 组去重数据、`samplecode.py` 和 `producecase.py`。
- 前 7 题取现有题解合集，后 13 题来自匹配失败候选，报告已标注；`index_tests.py` 后 catalog 为 `1254/1863` 有测试。
- 20/20 题真实 `judge.py` 参考解法均 `Accepted`；脚本 py_compile 通过。完整 unittest 受当前沙箱禁止监听 socket 影响，服务端测试报 `PermissionError: [Errno 1] Operation not permitted`。
- 待 Claude 复核题面约束、输出唯一性和无解/失败分支覆盖；当前报告明确 `no_solution_branch_covered=false`，没有虚报覆盖。

## 2026-07-25 · T-003 batch-002 首批生成

- 新增 `scripts/build_t003_002.py`，完成 manifest 首批 20 题；19 题各 20 组，22642 因 `N=1..10` 输入域有限做 10/10 穷尽覆盖。
- `producecase.py` 按新式生成器 + 固定种子落盘，不内嵌 `CASES`。逐题重跑后 `data/` SHA-256 逐字节不变，20/20 通过。
- 恒定输出探针 20/20 被拒；参考解法真实 judge AC 20/20；输入去重 20/20。索引后 catalog 为 `1300/1863` 有测试。
- 补齐 `collab/t002-001d-report.json` 中 10 个 `constraints: null` 的逐条约束表；本轮报告增加输出频率、恒定输出探针和 producecase 复现字段。

## 2026-07-25 · T-003 batch-002 选批

- 新增 `scripts/select_batch_002.py`，修正选批逻辑为排除全部既有 `_made` 目录和 special-judge 清单。
- 候选池实测 92 个；首批 20 题写入 `collab/t003-batch-002-manifest.json`，完整候选池写入 `collab/t003-batch-002-candidates.json`。
- 首批：`03424, 20744, 20746, 21509, 21515, 21535, 21728, 21759, 22067, 22068, 22158, 22161, 22271, 22359, 22485, 22491, 22508, 22509, 22636, 22642`。
- 仅完成选批和脚本语法检查，未生成测试数据；下一步按 001 的 20 题/轮流程逐题定制生成器。

## 2026-07-24 · T-001 完成

- 新增 `tests/` 标准库 `unittest` 回归套件，覆盖判题 AC/WA/TLE/RE/CE、token 语义、CPU/输出限制，以及服务端注册、登录、会话、未登录提交 401、认证提交和 URL 编码后的路径穿越。
- 修正 `judge.py` 的 Python CE 分类和 CPU 限制信号分类；`server.py` 支持 `CS101_DB` 隔离测试数据库，并在本地文件分发前拒绝解码后的 `..` 路径。
- 验证：`python3 -m unittest discover -s tests -p 'test_*.py'` 为 9/9；`python3 tools/handoff.py --verify` 为测试 9/9、py_compile 11 文件、node --check 全部通过。

## 2026-07-24 · T-002-001c 交付准备

- 001c 题号：`05804, 05902, 05907, 06250, 06263, 06640, 06901, 07161, 07207, 07218, 07576, 07734, 07743, 08758, 09198, 09201, 09202, 12029, 12757, 14683`。全部来自题解合集，出处、题面约束和输出唯一性检查写入 `collab/t002-001c-report.json`。
- 新增 `scripts/build_001c.py`。每题生成器固定题号种子；7218 的多组样例整体保留在 case-0；每题 report 标记 `constraints_checked` 和 `output_uniqueness_checked`。
- 特别处理：6250 避免题解中失配时会卡死的 KMP 路径，使用单字符且确定出现的模式；9201 从题解代码块中选择通过样例的完整实现；7207 批内拒绝重复输入。
- 验证：20 题去重均 20/20；独立 producecase + samplecode 20x20 一致；真实 judge 20/20 Accepted；追加输出变异 20/20 case 1 Wrong Answer；`index_tests.py` 后 catalog 为 1209/1863 有测试。

---

## 2026-07-24 · T-002 开工核验

- `scripts/index_tests.py` 现同时扫描题目目录顶层和 `data/` 子目录的 `.in/.out` 配对。
- 索引前后 catalog 有测试的记录为 `945 -> 978`，既有记录未减少；`4102_made=20` 组、`18250_made=10` 组已生效。
- `scripts/export_missing_tests.py` 生成 `collab/t002-missing-tests.json`：885 条无测试 catalog 记录，519 个去重本地题号。
- 当前工作区原有 `producecase_prompt/` 文件呈删除状态，且压缩包已不在盘上；本轮不恢复用户改动。20 题试点必须从剩余题面逐题产出 `samplecode.py`、`producecase.py` 和 `data/*.in|*.out`，不能把已有 `_made` 数据计入。

## 2026-07-24 · T-002 20 题试点交付

- 生成脚本：`scripts/build_t002_pilot.py`；每题目录均有 `samplecode.py`、`producecase.py` 和 `data/0..19.in|out`，组 0 为网页样例。
- 试点题号：`03468, 04117, 04118, 04137, 04138, 04146, 04148, 05345, 05455, 06646, 07622, 07810, 08581, 09267, 19757, 22275, 24637, 27217, 27880, 19943`。
- `producecase.py` 内置题号固定种子和样例断言；逐题重跑生成器后，20 组输入均由 `samplecode.py` 逐字节复算一致。
- 真实 `judge.py` 验收：20/20 题 `Accepted`，每题 20 组；索引重建后 catalog 为 1051/1863 有测试、11161 个配对。
- 多解/特殊判题候选见 `collab/t002-special-judge-skips.md`，本批不生成这些题。

## 2026-07-28 · T-010 Codex 复核收口

- `judge.py` 的 `prepare_program` 与父提交逐行核对，.NET 分支语义未漂移；本机 SDK 可用时真实测试受 NuGet 网络策略影响，放行验证已通过。
- `/api/run` 原先只校验登录，现先要求 `book/problem` 命中本地 catalog；未知题号返回 404，不进入沙箱。新增服务端回归用例。
- `tools/full_sweep.py` 新增标记式样例输出首行 `#` 的机械检查，当前实际扫描 253 题、零告警。
- `problems.html`、`history.html`、`admin.html` 补统一顶栏、题库/记录/说明导航、主题切换和登录用户链接。
- 复核结论：完整闸门退出码 0，101 项 unittest、py_compile、待返工检查和全库扫描均通过。

## 2026-07-24 · T-002-001a 交付核验

- 001a 取 manifest 前 20 个“题解代码可直接执行”题号：`03406, 03441, 03447, 03532, 03720, 04005, 04036, 04075, 04077, 04078, 04079, 04080, 04081, 04082, 04084, 04089, 04093, 04103, 04109, 04141`。
- 参考解法均原样取自题解合集，出处、题解文件、heading、seed、生成器名写入 `collab/t002-001a-report.json`；未使用自写参考解法。
- 每题包含 `samplecode.py`、`producecase.py`、20 组数据；生成器嵌入对应题解源码作为 reference solver，并先做网页样例断言。
- 验证：20/20 真实 judge Accepted；20/20 追加输出变异为 Wrong Answer 且 `case=1`；独立重跑后 20x20 输入/输出一致；索引重建后每题 `test_count=20`。

## 2026-07-24 · T-002-001a-fix · 四题去重补强

- 按复核意见只重做 `03447, 03720, 04081, 04082`，其余 16 题数据不改。
- `03447` 改为随机连通银河图（树边 + 随机补边、随机地球航线）；`03720` 改为随机一般二叉树并按缩进文本规则序列化；`04081` 改为随机一般树 DFS 合法 `u/d` 序列；`04082` 改为随机一般树、左儿子右兄弟转换、缺子节点补 `$` 后序列化。
- 四题均为 20/20 个不同输入；批内独立重跑后 20/20 题 samplecode 与输出一致，真实 judge 20/20 Accepted，追加输出变异 20/20 在 case 1 Wrong Answer。报告新增 `distinct_input_cases` 字段。

## 2026-07-24 · T-002-001b 交付准备

- 001b 取 001a 未覆盖的下一批 20 个题号：`04115, 04116, 04119, 04121, 04123, 04124, 04129, 04130, 04135, 04144, 04145, 04147, 04977, 05333, 05343, 05344, 05430, 05442, 05443, 05467`。参考解法均来自题解合集，逐题出处与约束清单写入 `collab/t002-001b-report.json`。
- 新增 `scripts/build_001b.py`，支持题解路径前缀映射、多个样例块取首个完整样例、逐题随机结构生成；每个 `producecase.py` 固定题号种子并嵌入同款参考解法。
- 题面约束已在报告的 `constraints` 逐条列出并由 `constraints_checked: true` 标记。重点结构：4130 方形迷宫/按序钥匙/终止行，5442 连通图/边数/度数上限，4124 含 `N=16` 上界组。
- 验证：20 题去重均 >=15（4123 为 16）；独立 producecase + samplecode 20x20 一致；真实 judge 20/20 Accepted；追加输出变异 20/20 case 1 Wrong Answer；`index_tests.py` 后 catalog 为 1161/1863 有测试。

## 2026-07-28 · problem_exists catalog 缓存

- 按复核建议把 `problem_exists()` 的 catalog 读取下沉缓存到 `judge.py`：只缓存 `(book, problem)` 键集合，按 `catalog.json` 的 `st_mtime_ns` 自动失效。
- 不复用 `server.py` 的 `catalog_raw()`，保持 judge 不反向依赖 server；解析失败仍返回不存在，未知题号的 404 语义不变。

## 2026-07-26 · T-002/T-003 平台回扫

- 对 T-002 80 题与 T-003 92 题的 `_made/samplecode.py` 做真实平台回扫，共 172 个去重目标。
- 结果：169 Accepted；23563 Wrong Answer；26999 Python3 与 PyPy3 均 Time Limit Exceeded；
  14683 因平台题页无 contestId，记为 NOT_SUBMITTABLE。
- 这次回扫证明人工题解来源也不能免除平台验证；T-002/T-003 当前不能标记为全量平台通过。
- 报告：`collab/t002-t003-platform-check-2026-07-26.json`。
## 2026-07-29 · T-026 统一题库页面

- `book.html` 是八个题库共用的极简浏览页：题目、排名、状态分别由根路径、`/ranking/`、`/status/` 打开。
- 首页题库标题直接指向对应根路径，确保题库入口实际进入这套 Tab 页面。
- 2026-07-29 样式复核后：参照本地 `pctbook__1.html` 的结构改成课程标题栏、横向活动 Tab、条纹表格与 50 题分页；颜色仍完全来自 `static/theme.css`，数据仍来自本机 `/api/books/<book>/`。
- `GET /api/books/<book>/` 只返回题目列表、聚合排名和最近 200 条提交摘要；它不复用需要认证的 `/api/submissions`，因此不会泄露 source 或 detail。
- 验证：新定向回归 2/2，`python3 tools/handoff.py --verify` 退出码 0，`git diff --check` 通过。
