__pycache__/
data/*.db
# WAL 模式下库旁边会多出这两个文件。`data/*.db` 匹配不到它们，
# 而 `-wal` 里就躺着尚未合并回主库的提交记录（含口令哈希那张表的写入）。
data/*.db-wal
data/*.db-shm
data/.admin_password
data/.smtp.env
# 抓取的第三方测试数据不入库；自产 *_made 与 GMyhf 自有 *_GMyhf 数据必须入库
data/openjudge/tests/**
!data/openjudge/tests/*/
!data/openjudge/tests/*/*_made/
!data/openjudge/tests/*/*_made/**
!data/openjudge/tests/*/*_GMyhf/
!data/openjudge/tests/*/*_GMyhf/**
# 上面两组 `!…/**` 把开头那条 `__pycache__/` 也一并翻了回来（后匹配者胜），
# 于是在 `_made` 目录里跑一次 `producecase.py` 就会有 .pyc 等着被 `git add -A` 收走。
data/openjudge/tests/*/*_made/__pycache__/
data/openjudge/tests/*/*_GMyhf/__pycache__/
data/openjudge/__MACOSX/
collab/review-input.md
# 手工起服务时的日志（systemd 部署走 journal，不产生这个文件）。
# 必须忽略：没配 SMTP 时服务端会把密码重置链接打进 stdout，
# 重定向进 server.log 就是一份可直接接管账号的凭据落在仓库目录里。
server.log
*.log
